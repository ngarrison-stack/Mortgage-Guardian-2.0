---
phase: 01-foundation-testing-infrastructure
plan: 02
type: execute
---

<objective>
Create comprehensive mock implementations for external service dependencies (Supabase, Plaid, Claude AI, Redis) to enable isolated unit testing without real API calls or network dependencies.

Purpose: Tests must run fast, deterministically, and without requiring API keys or external service availability. Mock services provide controlled, predictable responses for testing business logic in isolation.
Output: Reusable mock modules for all external dependencies with realistic response structures matching production API contracts.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-testing-infrastructure/01-01-SUMMARY.md
@backend-express/services/mockPlaidService.js
@backend-express/services/claudeService.js
@backend-express/services/plaidService.js

**Depends On:**
- Plan 01-01 (Jest configuration must be complete)

**Current State:**
- Existing mock: `mockPlaidService.js` demonstrates mock pattern
- Real services to mock:
  - `claudeService.js` - Anthropic Claude AI (@anthropic-ai/sdk)
  - `plaidService.js` - Plaid banking API (plaid package)
  - Supabase client (@supabase/supabase-js) - used in services
  - ioredis - Redis caching client

**Mock Pattern (from mockPlaidService.js):**
```javascript
const mockService = {
  methodName: async () => ({ /* mock response */ }),
  // ... additional methods
};
module.exports = mockService;
```

**Key Requirements:**
- Mocks return realistic data structures matching production APIs
- All methods return Promises (async-compatible)
- Configurable responses for testing different scenarios
- Error simulation capability
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Claude AI service mock</name>
  <files>backend-express/__tests__/mocks/mockClaudeService.js</files>
  <action>Create __tests__/mocks/mockClaudeService.js exporting mock matching claudeService.js interface:
- analyzeDocument({ documentText, documentType }) → returns { analysis: "Mock analysis result", confidence: 0.95, issues: [], timestamp: ISO_DATE }
- Implement configurable responses: mockClaudeService.setResponse(data) to override default
- Implement error simulation: mockClaudeService.setError(error) to throw on next call
- Include reset() method to restore defaults
- Match exact function signatures from real claudeService.js
- Use CommonJS exports (module.exports = { analyzeDocument, setResponse, setError, reset })</action>
  <verify>require('./backend-express/__tests__/mocks/mockClaudeService').analyzeDocument({ documentText: 'test' }) returns mock analysis object</verify>
  <done>mockClaudeService.js exports analyzeDocument with realistic response structure, configurable responses, and error simulation</done>
</task>

<task type="auto">
  <name>Task 2: Create Supabase client mock</name>
  <files>backend-express/__tests__/mocks/mockSupabaseClient.js</files>
  <action>Create __tests__/mocks/mockSupabaseClient.js exporting factory function mockSupabaseClient() that returns mock Supabase client:
- auth.getUser() → { data: { user: { id: 'mock-user-id', email: 'test@example.com' } }, error: null }
- from(table).select() → { data: [], error: null }
- from(table).insert(data) → { data: [data], error: null }
- from(table).update(data) → { data: [data], error: null }
- from(table).delete() → { data: [], error: null }
- Chainable query methods: .eq(), .neq(), .single()
- storage.from(bucket).upload() → { data: { path: 'mock-path' }, error: null }
- Include setResponse() and setError() for configuration
- Match @supabase/supabase-js v2 API structure</action>
  <verify>const client = mockSupabaseClient(); client.auth.getUser() returns user object</verify>
  <done>mockSupabaseClient.js provides factory returning mock client with auth, database (from/select/insert/update/delete), and storage methods</done>
</task>

<task type="auto">
  <name>Task 3: Create Redis client mock</name>
  <files>backend-express/__tests__/mocks/mockRedisClient.js</files>
  <action>Create __tests__/mocks/mockRedisClient.js exporting mock ioredis client:
- In-memory key-value store: Map to track set/get operations
- get(key) → returns stored value or null
- set(key, value, 'EX', seconds) → stores value with optional expiry
- del(key) → removes key, returns 1 if existed, 0 if not
- exists(key) → returns 1 if exists, 0 if not
- incr(key) → increments numeric value, returns new value
- expire(key, seconds) → sets TTL
- ttl(key) → returns remaining TTL or -1
- Clear() method to reset store between tests
- Match ioredis v5 API signatures (all methods return Promises)</action>
  <verify>const redis = mockRedisClient(); await redis.set('key', 'value'); await redis.get('key') === 'value'</verify>
  <done>mockRedisClient.js provides in-memory Redis mock with get/set/del/exists/incr/expire/ttl methods and test isolation via clear()</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All mock files created in `backend-express/__tests__/mocks/`
- [ ] Each mock exports expected API matching real service
- [ ] Mocks return realistic data structures
- [ ] Error simulation works (setError methods)
- [ ] No external API calls made from mocks
</verification>

<success_criteria>

- All tasks completed
- Three mock modules created (Claude, Supabase, Redis)
- Existing mockPlaidService.js pattern followed consistently
- Mocks enable isolated testing without API keys
- Configurable responses and error simulation available
- Ready for integration with actual test files
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-testing-infrastructure/01-02-SUMMARY.md`:

# Phase 1 Plan 2: Service Mocks Summary

**Comprehensive service mocks created for Supabase, Claude AI, Plaid, and Redis enabling isolated unit testing**

## Accomplishments

- Created mockClaudeService.js for Anthropic Claude AI with configurable responses
- Created mockSupabaseClient.js factory for database, auth, and storage operations
- Created mockRedisClient.js with in-memory key-value store
- Established mocking patterns for external dependencies

## Files Created/Modified

- `backend-express/__tests__/mocks/mockClaudeService.js` - Claude AI mock
- `backend-express/__tests__/mocks/mockSupabaseClient.js` - Supabase client factory mock
- `backend-express/__tests__/mocks/mockRedisClient.js` - Redis client mock

## Decisions Made

- Mock location: `__tests__/mocks/` directory for centralized test utilities
- Response configuration via setResponse/setError methods for flexibility
- In-memory storage for Redis mock (no external process required)
- Factory pattern for Supabase client (matches createClient() usage)

## Issues Encountered

[Document any issues or "None"]

## Next Step

Ready for 01-03-PLAN.md - Create integration test utilities and database fixtures
</output>
