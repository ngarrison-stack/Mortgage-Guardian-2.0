---
phase: 01-foundation-testing-infrastructure
plan: 03
type: execute
---

<objective>
Create reusable integration test utilities and database fixtures that enable comprehensive testing of API endpoints and service interactions with controlled, predictable test data.

Purpose: Integration tests require consistent test data, database setup/teardown, and helper functions for common testing patterns. These utilities eliminate repetitive test setup code and ensure reliable, reproducible test execution.
Output: Test utilities module with database fixtures, API request helpers, and integration test patterns ready for Phase 2-6 test development.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-testing-infrastructure/01-01-SUMMARY.md
@.planning/phases/01-foundation-testing-infrastructure/01-02-SUMMARY.md
@backend-express/server.js

**Depends On:**
- Plan 01-01 (Jest configuration complete)
- Plan 01-02 (Service mocks available)

**Current State:**
- Backend uses Supabase PostgreSQL (no direct database access in tests yet)
- Routes in `routes/` directory (API endpoints to test)
- Express app in `server.js` with middleware stack

**Integration Test Requirements:**
- Test Express routes without starting server (use supertest)
- Seed database with test fixtures
- Clean up test data after each test
- Generate valid JWT tokens for auth testing
- Helper functions for common assertions

**Tech Stack for Testing:**
- Jest (configured in 01-01)
- supertest (for HTTP testing without server startup)
- Service mocks (from 01-02)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install supertest for API testing</name>
  <files>backend-express/package.json</files>
  <action>Install supertest for HTTP assertion testing: `npm install --save-dev supertest@^7.x`. Supertest allows testing Express routes without starting a server. Do NOT install @types/supertest (not needed for JavaScript tests). Use --save-dev to add to devDependencies only.</action>
  <verify>npm list supertest shows package installed in devDependencies</verify>
  <done>supertest ^7.x present in backend-express/package.json devDependencies</done>
</task>

<task type="auto">
  <name>Task 2: Create test utilities module</name>
  <files>backend-express/__tests__/utils/testUtils.js</files>
  <action>Create __tests__/utils/testUtils.js exporting:
- setupTestApp() → returns Express app instance with all middleware but using mock services (inject mocks via dependency injection pattern or module mocking)
- generateTestJWT({ userId, email }) → returns valid JWT token using jsonwebtoken library (use JWT_SECRET from env or test secret 'test-secret-key')
- assertErrorResponse(response, statusCode, errorType) → validates error response format matches { error, message } structure
- assertSuccessResponse(response, expectedFields) → validates success response has { success: true, data } with expected fields
- Use CommonJS exports (module.exports = { setupTestApp, generateTestJWT, ... })</action>
  <verify>const { setupTestApp } = require('./backend-express/__tests__/utils/testUtils'); typeof setupTestApp === 'function'</verify>
  <done>testUtils.js exports helper functions for app setup, JWT generation, and response validation with consistent CommonJS exports</done>
</task>

<task type="auto">
  <name>Task 3: Create database fixtures</name>
  <files>backend-express/__tests__/fixtures/dbFixtures.js</files>
  <action>Create __tests__/fixtures/dbFixtures.js exporting fixture factory functions:
- createTestUser({ email = 'test@example.com', userId = 'test-user-id' } = {}) → returns user fixture object with id, email, created_at, updated_at
- createTestDocument({ userId, documentType = 'mortgage_statement' } = {}) → returns document fixture with id, user_id, document_type, status, created_at
- createTestAnalysis({ documentId, confidence = 0.95 } = {}) → returns analysis result fixture
- createTestTransaction({ amount = 1000, date = new Date() } = {}) → returns Plaid transaction fixture
- All fixtures return plain JavaScript objects (not database records - for use with mocked Supabase)
- Use realistic data structures matching production schema
- Include ISO 8601 timestamps via new Date().toISOString()</action>
  <verify>const { createTestUser } = require('./backend-express/__tests__/fixtures/dbFixtures'); createTestUser().email === 'test@example.com'</verify>
  <done>dbFixtures.js exports factory functions for user, document, analysis, and transaction fixtures with sensible defaults and customization options</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] supertest installed and importable
- [ ] testUtils.js provides app setup and JWT helpers
- [ ] dbFixtures.js provides fixture factories
- [ ] All utilities use CommonJS module pattern
- [ ] No external database connections required (uses mocks)
- [ ] Phase 1 complete - ready for Phase 2 auth testing
</verification>

<success_criteria>

- All tasks completed
- supertest available for HTTP testing
- Test utilities enable isolated API endpoint testing
- Database fixtures provide consistent test data
- Integration test patterns established
- Foundation complete for Phase 2-8 test development
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-testing-infrastructure/01-03-SUMMARY.md`:

# Phase 1 Plan 3: Integration Test Utilities Summary

**Integration test utilities and database fixtures created enabling comprehensive API and service testing**

## Accomplishments

- Installed supertest for HTTP testing without server startup
- Created testUtils.js with app setup, JWT generation, and response assertion helpers
- Created dbFixtures.js with factory functions for users, documents, analyses, and transactions
- Established integration testing patterns for subsequent phases

## Files Created/Modified

- `backend-express/package.json` - Added supertest dependency
- `backend-express/__tests__/utils/testUtils.js` - Test helper functions
- `backend-express/__tests__/fixtures/dbFixtures.js` - Fixture factories

## Decisions Made

- supertest for HTTP assertions (industry standard with Express)
- Test utilities location: `__tests__/utils/` for shared helpers
- Fixtures location: `__tests__/fixtures/` for test data factories
- JWT token generation using test secret for isolated auth testing
- Fixture factories return plain objects (not database inserts) for mock compatibility

## Issues Encountered

[Document any issues or "None"]

## Next Step

**Phase 1 Complete!** Ready for Phase 2: Authentication Layer
Next command: `/gsd:plan-phase 2` (after `/clear` for fresh context)
</output>
