---
phase: 01-foundation-testing-infrastructure
plan: 01
type: execute
---

<objective>
Establish Jest testing framework with TypeScript support, coverage reporting, and project-specific configuration optimized for Node.js Express backend testing.

Purpose: Create the foundation that all subsequent test development depends on. Without proper Jest configuration, tests cannot run reliably or produce meaningful coverage metrics.
Output: Working Jest setup with npm test command, coverage reporting, and TypeScript integration ready for test authoring.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/TESTING.md
@.planning/codebase/STACK.md
@.planning/codebase/CONVENTIONS.md
@backend-express/package.json

**Current State:**
- No test framework configured (test script exits 0 with "No tests yet")
- TypeScript 5.9.3 already in devDependencies
- Node.js >=20.0.0, npm >=9.0.0
- Existing test files: `test-claude.js`, `test-live-backend.sh` (manual integration tests)
- Mock service pattern already exists: `services/mockPlaidService.js`

**Key Decisions:**
- Jest selected as test framework (industry standard, excellent TypeScript support)
- Coverage targets: 90% minimum (per PROJECT.md active requirements)
- Test location: Follow Node.js convention with `__tests__/` directories

**Established Patterns:**
- camelCase.js for backend files
- Try/catch error handling
- async/await (no .then() chains)
- CommonJS modules (require/module.exports)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Jest and TypeScript dependencies</name>
  <files>backend-express/package.json</files>
  <action>Install Jest testing dependencies: `npm install --save-dev jest @types/jest ts-jest @jest/globals`. Do NOT install globally - project-local only. Use --save-dev to add to devDependencies. Versions: jest@^29.x (latest stable), @types/jest@^29.x, ts-jest@^29.x, @jest/globals@^29.x.</action>
  <verify>npm list jest @types/jest ts-jest @jest/globals shows all packages installed in devDependencies</verify>
  <done>All 4 packages present in backend-express/package.json devDependencies with compatible versions</done>
</task>

<task type="auto">
  <name>Task 2: Create Jest configuration file</name>
  <files>backend-express/jest.config.js</files>
  <action>Create jest.config.js with:
- preset: 'ts-jest' (for TypeScript support)
- testEnvironment: 'node' (not jsdom - this is backend)
- roots: ['<rootDir>'] (search from project root)
- testMatch: ['**/__tests__/**/*.test.js', '**/__tests__/**/*.test.ts'] (convention-based test discovery)
- collectCoverageFrom: ['services/**/*.js', 'routes/**/*.js', '!**/node_modules/**', '!**/__tests__/**'] (track service and route coverage, exclude tests and node_modules)
- coverageThreshold: { global: { statements: 90, branches: 90, functions: 90, lines: 90 } } (enforce 90% coverage per PROJECT.md)
- coverageReporters: ['text', 'lcov', 'html'] (console + CI-friendly + browsable report)
- moduleFileExtensions: ['js', 'ts'] (support both JavaScript and TypeScript)
- clearMocks: true (auto-reset mocks between tests)
- Do NOT use ES modules transform - keep CommonJS (project uses require/module.exports)</action>
  <verify>cat backend-express/jest.config.js shows valid configuration with all required fields</verify>
  <done>jest.config.js exists with Node.js test environment, TypeScript support, coverage thresholds, and proper test matching patterns</done>
</task>

<task type="auto">
  <name>Task 3: Update package.json test scripts</name>
  <files>backend-express/package.json</files>
  <action>Replace test script in scripts section:
- "test": "jest" (basic test runner)
- "test:watch": "jest --watch" (TDD mode)
- "test:coverage": "jest --coverage" (generate coverage report)
- "test:verbose": "jest --verbose" (detailed output for debugging)
Remove placeholder "echo 'No tests yet' && exit 0". Keep existing start, dev, vercel-build scripts unchanged.</action>
  <verify>npm run test --help shows Jest CLI options (confirms Jest is runnable)</verify>
  <done>package.json scripts section contains 4 test commands, all invoking jest with appropriate flags</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm test` runs Jest (even with 0 tests found)
- [ ] `npm run test:coverage` generates coverage report
- [ ] Jest config shows TypeScript support and Node.js environment
- [ ] No errors or warnings from Jest configuration
</verification>

<success_criteria>

- All tasks completed
- Jest framework operational with `npm test` command
- TypeScript integration working (ts-jest preset)
- Coverage reporting configured with 90% thresholds
- No breaking changes to existing code
- Ready for test file authoring in subsequent plans
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-testing-infrastructure/01-01-SUMMARY.md`:

# Phase 1 Plan 1: Jest Configuration Summary

**Jest testing framework configured with TypeScript support and 90% coverage thresholds**

## Accomplishments

- Installed Jest 29.x with TypeScript support (ts-jest, @types/jest, @jest/globals)
- Created jest.config.js with Node.js environment and coverage enforcement
- Updated npm test scripts (test, test:watch, test:coverage, test:verbose)

## Files Created/Modified

- `backend-express/package.json` - Added Jest dependencies and test scripts
- `backend-express/jest.config.js` - Created Jest configuration

## Decisions Made

- Test location: `__tests__/` directories (Node.js convention)
- Coverage threshold: 90% across statements/branches/functions/lines
- Test pattern: `**/__tests__/**/*.test.{js,ts}` for discovery
- Environment: node (not jsdom) for backend testing

## Issues Encountered

[Document any issues or "None"]

## Next Step

Ready for 01-02-PLAN.md - Create service mocks for Supabase, Plaid, Claude AI, and Redis
</output>
