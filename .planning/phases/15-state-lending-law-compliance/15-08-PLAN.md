---
phase: 15-state-lending-law-compliance
plan: 08
type: execute
---

<objective>
Extend compliance API routes with state statute endpoints and create integration tests for the full state compliance flow.

Purpose: Complete Phase 15 by exposing state compliance through the API and verifying end-to-end integration.
Output: Extended compliance routes with state endpoints, Joi schemas for state operations, and comprehensive integration tests.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior plans in this phase:
@.planning/phases/15-state-lending-law-compliance/15-01-SUMMARY.md
@.planning/phases/15-state-lending-law-compliance/15-07-SUMMARY.md

# Key source files:
@backend-express/routes/compliance.js
@backend-express/schemas/compliance.js
@backend-express/__tests__/complianceRoutes.test.js

**Established patterns:**
- Compliance API follows 200+error/404 pattern (14-06)
- Routes before param routes in server.js (12-03, 13-06, 14-06)
- Integration tests with mocked auth and services (14-06)
**Constraining decisions:**
- Phase 14-06: Statute endpoints at /v1/compliance/statutes, case compliance at /v1/cases/:caseId/compliance
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add state compliance Joi schemas and API routes</name>
  <files>backend-express/schemas/compliance.js, backend-express/routes/compliance.js</files>
  <action>Extend compliance schemas and routes for state statute support:

**Schemas (compliance.js):**
1. Add `listStateStatutesSchema` — validates query params: `state` (optional, 2-letter code), `category` (optional)
2. Add `getStateStatuteDetailsSchema` — validates params: `stateCode` (2-letter), `statuteId` (required)
3. Extend `evaluateComplianceSchema` — add optional fields: `state` (manual override), `skipStateAnalysis` (boolean), `stateStatuteFilter` (array of state statute IDs)
4. Add `getSupportedStatesSchema` — no params (just validates no garbage)
5. Export new schemas

**Routes (compliance.js):**
1. `GET /compliance/states` — list all supported states with statute counts
   - Response: `{ states: [{ stateCode, stateName, statuteCount, sectionCount }] }`
   - Uses stateStatuteTaxonomy.getSupportedStates()

2. `GET /compliance/states/:stateCode/statutes` — list statutes for a specific state
   - Response: `{ stateCode, stateName, statutes: [{ id, name, citation, enforcementBody, sectionCount }] }`
   - 404 if state not supported

3. `GET /compliance/states/:stateCode/statutes/:statuteId` — get detailed state statute
   - Response: full statute object with sections, requirements, violation patterns, penalties
   - 404 if state or statute not found

4. Existing `POST /cases/:caseId/compliance/evaluate` — already works (orchestrator handles state internally via 15-07 changes). Just ensure the extended options (state, skipStateAnalysis) flow through from the request body.

5. Existing `GET /cases/:caseId/compliance/report` — already returns the full report including state fields (from 15-07 orchestrator changes). No route changes needed.

Import stateStatuteTaxonomy at top of routes file. Add state routes AFTER existing statute routes but BEFORE case routes (maintaining the established pattern).

Apply `validate()` middleware and `requireAuth` to all new routes.</action>
  <verify>Run `node -e "require('./backend-express/routes/compliance')"` — no import errors. Run `npm test --prefix backend-express` to check no regressions.</verify>
  <done>3 new API endpoints for state statutes, extended evaluate schema with state options, all routes have auth + validation middleware.</done>
</task>

<task type="auto">
  <name>Task 2: Create integration tests for state compliance API</name>
  <files>backend-express/__tests__/complianceRoutes.test.js</files>
  <action>Add integration tests for the state compliance endpoints to the existing complianceRoutes.test.js:

1. **GET /v1/compliance/states tests (4 tests):**
   - Returns 401 without auth token
   - Returns 200 with list of supported states
   - Each state in response has stateCode, stateName, statuteCount
   - Response includes all 6 priority states

2. **GET /v1/compliance/states/:stateCode/statutes tests (4 tests):**
   - Returns 401 without auth token
   - Returns 200 with statutes for valid state (CA)
   - Returns 404 for unsupported state code
   - Each statute has id, name, citation, enforcementBody, sectionCount

3. **GET /v1/compliance/states/:stateCode/statutes/:statuteId tests (4 tests):**
   - Returns 401 without auth token
   - Returns 200 with detailed statute for valid state + statute
   - Returns 404 for invalid state code
   - Returns 404 for invalid statute ID within valid state

4. **POST /v1/cases/:caseId/compliance/evaluate with state options (3 tests):**
   - Evaluate with manual state override — state option flows to orchestrator
   - Evaluate with skipStateAnalysis=true — no state violations in response
   - Evaluate with stateStatuteFilter — filters applied

5. **GET /v1/cases/:caseId/compliance/report with state data (2 tests):**
   - Report includes jurisdiction field when state analysis was run
   - Report includes stateViolations array

Follow existing test patterns: mock requireAuth, mock services, use supertest.

Target: ~17 new integration tests.</action>
  <verify>Run `npm test --prefix backend-express -- --testPathPattern=complianceRoutes` — all existing + new tests pass.</verify>
  <done>~17 new integration tests added. Total compliance route tests: ~38. All tests pass with zero regressions.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm test --prefix backend-express` passes (all tests)
- [ ] 3 new state statute API endpoints respond correctly
- [ ] State options flow through evaluate endpoint
- [ ] Reports include state compliance data
- [ ] ~17 new integration tests pass
- [ ] No regressions in existing test suites
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Phase 15 complete: State Lending Law Compliance Engine fully implemented
- 6 priority states with statutes, rules, jurisdiction detection, AI analysis, API endpoints
  </success_criteria>

<output>
After completion, create `.planning/phases/15-state-lending-law-compliance/15-08-SUMMARY.md`

Final summary should note:
- Phase 15 COMPLETE
- Total state statutes and sections added
- Total test count
- Ready for Phase 16: Consolidated Findings & Reporting
</output>
