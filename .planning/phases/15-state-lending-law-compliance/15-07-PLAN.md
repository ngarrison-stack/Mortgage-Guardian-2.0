---
phase: 15-state-lending-law-compliance
plan: 07
type: execute
---

<objective>
Extend the Claude AI compliance analysis service and the compliance orchestrator to include state law evaluation.

Purpose: Wire state compliance into the existing pipeline — the AI service needs state-specific prompts, and the orchestrator needs a state evaluation step alongside the federal step.
Output: Extended complianceAnalysisService.js with state context, extended complianceService.js orchestrator with state evaluation step, and unit tests for both.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior plans in this phase:
@.planning/phases/15-state-lending-law-compliance/15-01-SUMMARY.md
@.planning/phases/15-state-lending-law-compliance/15-02-SUMMARY.md
@.planning/phases/15-state-lending-law-compliance/15-05-SUMMARY.md
@.planning/phases/15-state-lending-law-compliance/15-06-SUMMARY.md

# Key source files:
@backend-express/services/complianceAnalysisService.js
@backend-express/services/complianceService.js
@backend-express/__tests__/complianceService.test.js
@backend-express/services/jurisdictionService.js

**Established patterns:**
- Orchestrator step pattern with graceful degradation (14-05)
- Best-effort persistence (10-04, 13-05, 14-05)
- Claude AI batched analysis with temperature 0.1 (14-04)
**Constraining decisions:**
- Phase 14-04: Violations batched by statute (max 10 per call)
- Phase 14-05: 5-step pipeline, error objects over exceptions, AI auto-skip on zero violations
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend compliance analysis service for state violation context</name>
  <files>backend-express/services/complianceAnalysisService.js</files>
  <action>Extend ComplianceAnalysisService to handle state violations alongside federal:

1. Add `analyzeStateViolations(stateViolations, caseContext)` method:
   - Same batching pattern as federal analyzeViolations() (max 10 per call)
   - State-specific prompt additions: include state name, state statute citation, state-specific legal context
   - Use same model (claude-sonnet-4-5) and temperature (0.1)
   - Returns { enhancedViolations, analysisMetadata } — same shape as federal

2. Extend `generateLegalNarrative()` to accept optional `stateViolations` parameter:
   - If state violations present, add a "State Law Analysis" section to the narrative
   - List applicable states and their specific violations
   - Reference state-specific penalties and enforcement bodies

3. Add `_buildStateViolationPrompt(violation)` private method:
   - Uses getStateStatuteById() and getStateSectionById() from stateStatuteTaxonomy
   - Includes state-specific legal context (state enforcement body, state penalties)
   - Otherwise follows same prompt structure as federal _buildViolationAnalysisPrompt()

Import stateStatuteTaxonomy helpers at top of file.

Do NOT modify existing federal methods — add new methods alongside them. analyzeViolations() continues to handle federal only.</action>
  <verify>Run `node -e "const s = require('./backend-express/services/complianceAnalysisService'); console.log(typeof s.analyzeStateViolations);"` — should show 'function'.</verify>
  <done>complianceAnalysisService.js has analyzeStateViolations(), extended generateLegalNarrative() with state section, state-specific prompts. Existing federal methods unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Extend compliance orchestrator with state evaluation step and tests</name>
  <files>backend-express/services/complianceService.js, backend-express/__tests__/complianceService.test.js</files>
  <action>Extend ComplianceService orchestrator to include state compliance evaluation:

1. **Extend evaluateCompliance()** — add new steps between existing Step 2 and Step 3:
   - **Step 2a: DETECT JURISDICTION** — call jurisdictionService.detectJurisdiction(caseData, options)
   - **Step 2b: STATE RULE ENGINE** — if jurisdiction.applicableStates.length > 0, call complianceRuleEngine.evaluateStateFindings(forensicReport, analysisReports, jurisdiction)
   - **Step 3a: STATE AI ENHANCEMENT** — if state violations found and !skipAiAnalysis, call complianceAnalysisService.analyzeStateViolations()

2. **Extend report assembly (Step 4):**
   - Add `jurisdiction` field from jurisdiction detection result
   - Add `stateViolations` array from state rule engine (enhanced by AI if available)
   - Add `stateStatutesEvaluated` from state rule engine result
   - Add `stateCompliance` summary: { statesAnalyzed, totalStateViolations, stateRiskLevel }
   - Keep existing federal fields unchanged

3. **Options extension:**
   - Add `options.state` — manual state override passed to jurisdictionService
   - Add `options.skipStateAnalysis` — skip state compliance entirely (federal only)
   - `options.statuteFilter` continues to work for federal; add `options.stateStatuteFilter` for state

4. **Graceful degradation:**
   - Jurisdiction detection failure → log warning, skip state analysis, continue with federal only
   - State rule engine failure → log warning, include jurisdiction but no state violations
   - State AI failure → return un-enhanced state violations (same pattern as federal)

5. **Update unit tests** in complianceService.test.js:
   - Add mock for jurisdictionService.detectJurisdiction
   - Add mock for complianceRuleEngine.evaluateStateFindings
   - Add mock for complianceAnalysisService.analyzeStateViolations
   - Test cases: state analysis happy path, skipStateAnalysis option, jurisdiction failure degradation, state rule engine failure degradation, state AI failure degradation, manual state override
   - Target: 8-10 new tests

Import jurisdictionService at top with lazy require (consistent with caseFileService pattern).</action>
  <verify>Run `npm test --prefix backend-express -- --testPathPattern=complianceService` — all existing + new tests pass.</verify>
  <done>ComplianceService orchestrator has jurisdiction detection + state rule engine + state AI steps. 8-10 new tests covering state evaluation paths. All existing tests still pass.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm test --prefix backend-express` passes (all tests including new ones)
- [ ] complianceAnalysisService has analyzeStateViolations() and state narrative support
- [ ] complianceService orchestrator runs both federal and state evaluation
- [ ] Graceful degradation tested for jurisdiction, state rules, and state AI failures
- [ ] Report output includes jurisdiction and stateViolations fields
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Federal compliance flow unchanged (backward compatible)
- State compliance integrated with graceful degradation at every step
- 8-10 new unit tests for state paths
  </success_criteria>

<output>
After completion, create `.planning/phases/15-state-lending-law-compliance/15-07-SUMMARY.md`
</output>
