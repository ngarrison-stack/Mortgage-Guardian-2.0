---
phase: 34-document-upload-processing-pipeline
plan: 01
type: execute
---

<objective>
Wire the document capture flow to upload captured documents to the Express backend and trigger the server-side processing pipeline.

Purpose: Currently DocumentCaptureViewModel processes documents locally via DocumentProcessor (on-device OCR only) but never uploads to the Express backend. This plan connects capture → upload → pipeline trigger so documents flow through the full backend analysis pipeline.
Output: Captured documents upload to Express backend and trigger server-side OCR → classify → analyze pipeline.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phase context:
@.planning/phases/32-express-api-client-migration/32-02-SUMMARY.md
@.planning/phases/32-express-api-client-migration/32-03-SUMMARY.md
@.planning/phases/33-authentication-flow-completion/33-02-SUMMARY.md

# Key source files:
@MortgageGuardian/ViewModels/DocumentCaptureViewModel.swift
@MortgageGuardian/Services/DocumentProcessor.swift
@MortgageGuardian/Services/DocumentManager.swift
@MortgageGuardian/Services/APIClient+Documents.swift

**Tech stack available:** APIClient with retry logic, @Observable, Clerk JWT auth, APIClient+Documents with uploadDocument/processDocument/getDocumentStatus methods
**Established patterns:** OCR-first pattern (iOS Vision OCR → text → Express), APIClient extension pattern, base64 upload encoding
**Constraining decisions:**
- [Phase 32-03]: DocumentAnalysisService sends OCR text (not image) to Express — iOS does OCR locally via Vision
- [Phase 32-02]: ExpressDocument type exists separately from MortgageDocument to avoid breaking views
- [Phase 33-02]: APIClient has auth token set by AuthManager, 401 callback for re-auth
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire DocumentCaptureViewModel to upload after local processing</name>
  <files>MortgageGuardian/ViewModels/DocumentCaptureViewModel.swift, MortgageGuardian/Services/DocumentManager.swift</files>
  <action>
In DocumentCaptureViewModel.processSelectedDocument(), after documentProcessor.processDocument() succeeds and creates a local MortgageDocument:
1. Extract the OCR text from the processed MortgageDocument (originalText field)
2. Get the image data (already available as imageData in the method)
3. Call APIClient.shared.uploadDocument() with base64-encoded image data, the OCR text in metadata, document type, and a generated filename
4. On upload success, store the returned server document ID on the MortgageDocument (or in a local mapping) so we can reference it later for status polling and analysis

Similarly wire processBatchDocuments() to upload each processed document after local OCR.

Do NOT replace the local DocumentProcessor OCR step — keep it. The flow is: local OCR → upload (with OCR text) → backend pipeline. This preserves the cost-saving Vision Framework first approach.

Add an @Published uploadProgress property to track upload state for UI feedback.
  </action>
  <verify>Build the Xcode project — no compiler errors. DocumentCaptureViewModel.processSelectedDocument() now calls APIClient.uploadDocument() after local processing.</verify>
  <done>processSelectedDocument() uploads to Express backend after local OCR. Upload response document ID is captured. processBatchDocuments() also uploads each document.</done>
</task>

<task type="auto">
  <name>Task 2: Trigger backend processing pipeline after upload</name>
  <files>MortgageGuardian/ViewModels/DocumentCaptureViewModel.swift, MortgageGuardian/Services/APIClient+Documents.swift</files>
  <action>
After successful upload in processSelectedDocument():
1. Call APIClient.shared.processDocument(documentId:) to trigger the Express pipeline (POST /v1/documents/process)
2. The backend pipeline runs: OCR → classify → analyze (server-side)
3. Log the pipeline trigger response
4. Update processingMessage to inform user: "Document uploaded — analysis in progress"
5. Set processingState to .completed (the pipeline runs async on backend)

Check APIClient+Documents.processDocument() method signature — ensure it accepts the document ID returned from upload. If the method signature doesn't match (e.g., it expects different params), adjust the call to match what the Express POST /v1/documents/process endpoint expects.

Do NOT block on pipeline completion — it runs asynchronously on the backend. Status polling will be added in Plan 34-03.

Handle errors gracefully: if upload succeeds but pipeline trigger fails, still show the document as uploaded (it can be re-triggered later via the analyze button in DocumentsView).
  </action>
  <verify>Build the Xcode project — no compiler errors. After capture + OCR + upload, processDocument API call is made to trigger backend pipeline.</verify>
  <done>Backend pipeline is triggered after upload. Errors handled gracefully — upload success preserved even if pipeline trigger fails. User sees "analysis in progress" feedback.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Xcode build succeeds without errors
- [ ] processSelectedDocument() flow: local OCR → upload → pipeline trigger
- [ ] processBatchDocuments() flow: each doc gets local OCR → upload
- [ ] Upload errors don't crash — graceful error handling
- [ ] Pipeline trigger failure doesn't lose the uploaded document
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No compiler errors introduced
- Document capture flow connects to Express backend end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/34-document-upload-processing-pipeline/34-01-SUMMARY.md`
</output>
