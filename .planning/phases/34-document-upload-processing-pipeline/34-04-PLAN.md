---
phase: 34-document-upload-processing-pipeline
plan: 04
type: execute
---

<objective>
Wire the document analysis trigger button and display analysis results from the Express backend.

Purpose: DocumentsView has a stubbed analyzeDocument() (TODO at line 218) and no analysis results display (TODO at line 406). This plan connects both — users can trigger analysis and see findings.
Output: Analyze button triggers backend pipeline. Analysis results display in document detail.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/34-document-upload-processing-pipeline/34-01-SUMMARY.md
@.planning/phases/34-document-upload-processing-pipeline/34-02-SUMMARY.md
@.planning/phases/34-document-upload-processing-pipeline/34-03-SUMMARY.md

# Key source files:
@MortgageGuardian/Views/DocumentsView.swift
@MortgageGuardian/Services/APIClient+Documents.swift
@MortgageGuardian/Models/MortgageDocument.swift

**Tech stack available:** APIClient.shared.processDocument(documentId:) triggers pipeline, APIClient.shared.getDocumentAnalysis(documentId:) returns structured analysis results (ExpressDocumentAnalysisResponse).
**Established patterns:** SwiftUI NavigationLink for detail views, @Published state management, Express analysis response includes findings array with confidence scores
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire analyzeDocument() to trigger backend analysis pipeline</name>
  <files>MortgageGuardian/Views/DocumentsView.swift</files>
  <action>
Replace the stubbed analyzeDocument() method (line 218, currently just a print statement) with:

1. Guard that the document has a serverDocumentId — if nil, show an alert that the document needs to be uploaded first
2. Call APIClient.shared.processDocument(documentId:) to trigger the backend pipeline
3. Show a brief loading indicator or toast: "Analysis started..."
4. Update the document's pipelineStatus to "uploaded" (or whatever the pipeline trigger returns) so the polling from Plan 34-03 picks it up and starts tracking progress
5. Handle errors: show user-friendly alert if pipeline trigger fails (network error, auth error, etc.)

Make the method async — wrap the call site in Task { } since it's called from a button action.

Also update the swipe action button that calls analyzeDocument (around line 197-201) to show a loading state while the API call is in progress (disable the button, optionally show spinner).
  </action>
  <verify>Build succeeds. analyzeDocument() makes API call instead of printing. Error handling shows alert on failure.</verify>
  <done>Analyze button triggers backend pipeline via API. Loading state shown during API call. Errors surface as user-friendly alerts. Polling picks up new pipeline document.</done>
</task>

<task type="auto">
  <name>Task 2: Display analysis results in document detail</name>
  <files>MortgageGuardian/Views/DocumentsView.swift</files>
  <action>
1. Read the ExpressDocumentAnalysisResponse struct in APIClient+Documents.swift to understand what fields are returned (findings, confidence scores, document type classification, etc.)

2. In DocumentsView, for analyzed documents (isAnalyzed == true), replace the "TODO: Add detailed analysis results" comment (line 406) with actual content:
   - When user taps an analyzed document row, fetch analysis from APIClient.shared.getDocumentAnalysis(documentId:)
   - Display in an expandable section or NavigationLink detail:
     * Document type classification
     * Number of findings/issues detected
     * List of findings with severity and confidence score
     * Brief description of each finding

3. Use a simple inline expansion (DisclosureGroup) or sheet for the analysis detail — do NOT create a separate view file (keep it lightweight, Phase 35 handles full analysis UI).

4. Cache fetched analysis results on the MortgageDocument (add an `analysisResults` or `serverAnalysis` property) to avoid re-fetching on every tap.

Keep the existing document row layout intact. Add the analysis detail as additional content below the existing row info, shown on tap/expand.
  </action>
  <verify>Build succeeds. Tapping an analyzed document shows analysis results fetched from backend. Findings display with severity and confidence.</verify>
  <done>Analyzed documents show findings on tap. Analysis fetched from getDocumentAnalysis() endpoint. Results cached locally. Unanalyzed documents don't show analysis section.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Xcode build succeeds without errors
- [ ] Analyze button triggers backend pipeline (no more TODO)
- [ ] Analyzed documents show real analysis results (no more TODO)
- [ ] Analysis results include findings with confidence scores
- [ ] Error handling for both trigger and fetch operations
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No compiler errors introduced
- Both document-related TODOs in DocumentsView resolved
</success_criteria>

<output>
After completion, create `.planning/phases/34-document-upload-processing-pipeline/34-04-SUMMARY.md`
</output>
