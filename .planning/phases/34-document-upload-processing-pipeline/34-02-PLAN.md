---
phase: 34-document-upload-processing-pipeline
plan: 02
type: execute
---

<objective>
Sync the DocumentsView document list with the Express backend so users see their server-stored documents, not just locally-cached ones.

Purpose: DocumentsView currently displays userStore.documents (local array). After Plan 34-01 uploads documents to Express, the list needs to reflect backend state — including documents uploaded from other sessions or devices.
Output: DocumentsView fetches and displays documents from Express backend with proper model bridging.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/34-document-upload-processing-pipeline/34-01-SUMMARY.md

# Key source files:
@MortgageGuardian/Views/DocumentsView.swift
@MortgageGuardian/Services/APIClient+Documents.swift
@MortgageGuardian/Models/MortgageDocument.swift

**Tech stack available:** APIClient.shared.fetchDocuments() returns ExpressDocumentListResponse with [ExpressDocument], UserStore holds [MortgageDocument]
**Established patterns:** @EnvironmentObject userStore, ExpressDocument type separate from MortgageDocument (Phase 32-02 decision)
**Constraining decisions:**
- [Phase 32-02]: New ExpressDocument type avoids breaking existing view models — must map to MortgageDocument for UI
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fetch documents from Express backend on view appear and pull-to-refresh</name>
  <files>MortgageGuardian/Views/DocumentsView.swift, MortgageGuardian/Models/UserStore.swift</files>
  <action>
1. Find UserStore (likely in Models/ or Services/ — grep for "class UserStore"). Add a method `fetchDocumentsFromBackend()` that:
   - Calls APIClient.shared.fetchDocuments()
   - Maps the returned [ExpressDocument] to [MortgageDocument] (see Task 2 for mapping)
   - Updates the @Published documents array
   - Sets isLoading during fetch

2. In DocumentsView, add .task { } modifier (or .onAppear) to call userStore.fetchDocumentsFromBackend() when the view appears.

3. Add pull-to-refresh via .refreshable { } modifier on the documents list that re-fetches from backend.

4. Keep existing local documents as fallback — if backend fetch fails, show cached local documents with a subtle error banner (not a blocking alert).

Do NOT remove the existing local document management — it serves as offline cache. Merge strategy: backend documents are source of truth when available.
  </action>
  <verify>Build succeeds. DocumentsView shows .task modifier or .onAppear that triggers backend fetch. Pull-to-refresh modifier present on list.</verify>
  <done>DocumentsView fetches from Express on appear. Pull-to-refresh works. Fetch errors show non-blocking feedback. Local cache used as fallback.</done>
</task>

<task type="auto">
  <name>Task 2: Bridge ExpressDocument to MortgageDocument model mapping</name>
  <files>MortgageGuardian/Models/MortgageDocument.swift, MortgageGuardian/Services/APIClient+Documents.swift</files>
  <action>
Create an extension on MortgageDocument (or a static factory method) that converts an ExpressDocument to a MortgageDocument:

1. Read the ExpressDocument struct in APIClient+Documents.swift to understand its fields (id, documentId, fileName, documentType, status, createdAt, etc.)
2. Read MortgageDocument to understand its required fields (fileName, documentType enum, uploadDate, originalText, extractedData, analysisResults, isAnalyzed)
3. Create `MortgageDocument.init(from expressDocument: ExpressDocument)` or `extension` that maps:
   - fileName from ExpressDocument.fileName
   - documentType: map string → MortgageDocument.DocumentType enum (with .other fallback)
   - uploadDate: parse ISO8601 string from createdAt
   - isAnalyzed: derive from status == "analyzed" or "complete"
   - originalText: from ExpressDocument metadata or empty string if not available
   - Store the server ID (ExpressDocument.id or documentId) so we can reference it for API calls

4. Also add a `serverDocumentId: String?` property to MortgageDocument if it doesn't exist — needed to call getDocumentAnalysis(), deleteDocument(), etc. by server ID.

Keep backward compatibility — existing local MortgageDocuments (without server IDs) should still work.
  </action>
  <verify>Build succeeds. MortgageDocument can be initialized from an ExpressDocument. serverDocumentId property exists on MortgageDocument.</verify>
  <done>ExpressDocument → MortgageDocument mapping works. Document type string maps to enum. Server document ID stored for API calls. Backward compatible with existing local documents.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Xcode build succeeds without errors
- [ ] DocumentsView loads documents from backend on appear
- [ ] Pull-to-refresh triggers backend re-fetch
- [ ] ExpressDocument maps cleanly to MortgageDocument
- [ ] MortgageDocument has serverDocumentId for API reference
- [ ] Backend fetch failure falls back to local cache gracefully
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No compiler errors introduced
- Documents list reflects backend state
</success_criteria>

<output>
After completion, create `.planning/phases/34-document-upload-processing-pipeline/34-02-SUMMARY.md`
</output>
