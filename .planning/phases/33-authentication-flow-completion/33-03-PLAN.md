---
phase: 33-authentication-flow-completion
plan: 03
type: execute
---

<objective>
Verify end-to-end auth flow compiles and update LoginView with proper error handling and sign-up flow cleanup.

Purpose: LoginView has basic sign-in/sign-up but needs error handling improvements and the sign-up flow needs to work with the updated AuthManager. Verify the complete auth chain compiles.
Output: Polished LoginView, verified compilation, phase 33 complete.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-authentication-flow-completion/33-01-SUMMARY.md
@.planning/phases/33-authentication-flow-completion/33-02-SUMMARY.md

**Key files:**
@MortgageGuardian/Views/LoginView.swift
@MortgageGuardian/Services/AuthManager.swift

**Current state of LoginView:**
- Has email/password fields, sign-in/sign-up toggle
- Calls authManager.signIn() / authManager.signUp()
- Shows loading spinner and error alerts
- No handling for the new isLoading state from AuthManager
- No specific error messages for auth failures (generic error string)

**Constraining decisions:**
- Clerk iOS SDK v0.71.4
- AuthManager now has isLoading, refreshToken(), handleAuthenticationRequired() (from 33-02)
- App entry handles loading/auth/main routing (from 33-02)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update LoginView error handling and sign-up flow</name>
  <files>MortgageGuardian/Views/LoginView.swift</files>
  <action>
Update LoginView with:

1. **Better error presentation:**
   - Map common Clerk errors to user-friendly messages:
     - "identifier is invalid" → "Invalid email address"
     - "password" errors → "Incorrect password" or "Password must be at least 8 characters"
     - Network errors → "Unable to connect. Check your internet connection."
     - Generic fallback → "Sign in failed. Please try again."
   - Show error text inline below the form (not just an alert)

2. **Sign-up flow cleanup:**
   - After successful signUp(), show a message that email verification is needed
   - Add a `@State private var showingVerification = false` flag
   - When verification needed, show text: "Check your email for a verification link, then sign in."
   - Toggle back to sign-in mode after showing verification message

3. **Disable form during loading:**
   - Disable email/password fields and button when `isLoading` is true
   - Use `.disabled(isLoading)` on the form section

4. **Keyboard handling:**
   - Add `.submitLabel(.go)` on password field
   - Add `.onSubmit` to trigger sign-in/sign-up

Keep the visual design as-is — don't change colors, fonts, or layout structure. Only improve the interaction logic.
  </action>
  <verify>File compiles. LoginView shows inline errors. Sign-up shows verification message. Form disables during loading.</verify>
  <done>LoginView has user-friendly error messages, sign-up verification guidance, disabled state during loading, keyboard submit.</done>
</task>

<task type="auto">
  <name>Task 2: Build verification and DocumentManager Clerk import cleanup</name>
  <files>MortgageGuardian/Services/DocumentManager.swift</files>
  <action>
1. **Remove unused Clerk import from DocumentManager.swift:**
   - Line 5 has `import Clerk` but Clerk is no longer used in this file (auth token management moved to APIClient in Phase 32)
   - Remove the import line

2. **Verify Xcode build:**
   Run: `xcodebuild -workspace MortgageGuardian.xcworkspace -scheme MortgageGuardian -destination 'platform=iOS Simulator,name=iPhone 17 Pro,OS=latest' build 2>&1 | grep -E "error:|BUILD SUCCEEDED|BUILD FAILED" | head -30`

   Fix any compilation errors found. Common expected issues:
   - Type mismatches from AuthManager changes
   - Missing imports
   - Renamed methods

3. **Verify backend tests still pass:**
   Run: `cd backend-express && npm test 2>&1 | tail -10`

   All 1636+ tests must pass (including new auth middleware tests from 33-01).
  </action>
  <verify>Xcode build passes (or only pre-existing non-migration errors). Backend tests pass. No unused imports.</verify>
  <done>DocumentManager cleaned up. Build verified. Backend tests pass. Phase 33 complete — full auth flow from Clerk iOS → Express backend.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] Express backend accepts Clerk JWTs (from 33-01)
- [ ] iOS AuthManager handles full token lifecycle (from 33-02)
- [ ] 401 triggers automatic re-auth attempt (from 33-02)
- [ ] LoginView shows user-friendly errors
- [ ] Backend tests pass (1636+)
- [ ] Xcode build succeeds (migration-related errors only)
</verification>

<success_criteria>

- Complete auth chain: Clerk iOS → JWT → Express middleware → authenticated API
- Token lifecycle: issue → refresh → expire → re-auth
- User-friendly auth errors in LoginView
- No unused imports or dead code from auth migration
- Phase 33 complete
</success_criteria>

<output>
After completion, create `.planning/phases/33-authentication-flow-completion/33-03-SUMMARY.md`
</output>
