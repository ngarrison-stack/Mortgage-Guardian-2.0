---
phase: 06-document-processing-tests
plan: 02
type: execute
---

<objective>
Write route handler tests for all document endpoints (GET /, GET /:id, DELETE /:id) via supertest. POST /upload is already covered by documents-upload-security.test.js.

Purpose: routes/documents.js is at 46.66% coverage — only POST /upload is tested. GET list, GET single, DELETE endpoints are uncovered. This plan completes the route coverage.
Output: `__tests__/routes/documents-routes.test.js` (~15 tests).
</objective>

<context>
@backend-express/routes/documents.js
@backend-express/schemas/documents.js
@backend-express/__tests__/routes/auth-integration.test.js (reference pattern for supertest setup)
@backend-express/__tests__/routes/documents-upload-security.test.js (reference pattern)
</context>

<tasks>
<task type="auto">
  <name>Task 1: Document route handler tests</name>
  <files>backend-express/__tests__/routes/documents-routes.test.js</files>
  <action>
    Create test file using supertest against the Express app.
    Follow the same auth mock pattern from auth-integration.test.js.

    Mock setup:
    - Mock @supabase/supabase-js for auth middleware
    - Mock documentService methods to control responses
    - Mock other services (claude, plaid) to prevent startup errors

    Test cases:

    GET /v1/documents:
    1. "returns 200 with documents array" — mock getDocumentsByUser to return docs
    2. "passes userId, limit, offset to service" — verify query params forwarded
    3. "validates userId is required" — omit userId, expect 400
    4. "returns 500 on service error" — mock service to throw

    GET /v1/documents/:documentId:
    5. "returns 200 with document data" — mock getDocument to return doc
    6. "returns 404 when document not found" — mock getDocument returns null
    7. "validates userId query param required" — omit userId, expect 400
    8. "returns 500 on service error" — mock service to throw

    DELETE /v1/documents/:documentId:
    9. "returns 200 with success message" — mock deleteDocument succeeds
    10. "validates userId query param required" — omit userId, expect 400
    11. "returns 500 on service error" — mock service to throw

    Run full suite.
  </action>
  <verify>npx jest __tests__/routes/documents-routes.test.js --verbose && npx jest --coverage</verify>
  <done>11+ route test cases pass, routes/documents.js >= 90% coverage</done>
</task>
</tasks>

<verification>
- [ ] `npx jest __tests__/routes/documents-routes.test.js` passes all tests
- [ ] `npx jest` (full suite) passes — no regressions
- [ ] Combined coverage of routes/documents.js >= 90%
</verification>
