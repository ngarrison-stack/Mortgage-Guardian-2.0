---
phase: 06-document-processing-tests
plan: 01
type: execute
---

<objective>
Write comprehensive unit tests for documentService.js covering all CRUD operations in both Supabase and mock modes, plus the getContentType helper.

Purpose: documentService.js is at 39.77% coverage — only uploadDocument's happy path is tested via the upload security integration tests. All other methods (getDocumentsByUser, getDocument, deleteDocument) and all error/edge paths are uncovered.
Output: `__tests__/services/documentService.test.js` (~30 tests).
</objective>

<context>
@backend-express/services/documentService.js
@backend-express/jest.config.js
@backend-express/__tests__/services/plaidDataService.test.js (reference pattern)
</context>

<tasks>
<task type="auto">
  <name>Task 1: documentService unit tests</name>
  <files>backend-express/__tests__/services/documentService.test.js</files>
  <action>
    Create test file using the same chainable Supabase mock pattern from plaidDataService tests.

    IMPORTANT: documentService uses BOTH supabase.from() chains AND supabase.storage.from() chains.
    Must mock both.

    Mock setup:
    - Chainable DB mock: from -> insert/select/update/delete -> eq -> single/order/range
    - Storage mock: storage.from('documents') -> upload/download/remove
    - Build object first, wire self-references after (avoid init-time self-reference)

    Supabase mode test cases:

    uploadDocument:
    1. "uploads file to storage and saves metadata" — mock storage.upload + db insert success
    2. "converts base64 content to Buffer for storage" — verify Buffer.from called
    3. "throws on storage error" — mock storage.upload to return error
    4. "throws on database error" — mock db insert to return error

    getDocumentsByUser:
    5. "returns documents filtered by userId" — mock select/eq/order/range chain
    6. "applies limit and offset" — verify range() called with correct args
    7. "returns empty array when no documents" — mock returns null data
    8. "throws on database error" — mock returns error

    getDocument:
    9. "returns metadata and base64 content" — mock db single + storage download
    10. "returns null when document not found" — mock returns null metadata
    11. "returns metadata only when download fails" — mock storage download error
    12. "throws on database error" — mock db returns error

    deleteDocument:
    13. "deletes from storage and database" — mock both succeed
    14. "throws when document not found" — mock db returns null
    15. "continues delete when storage deletion fails" — mock storage error, verify db delete still called
    16. "throws on database delete error" — mock db delete error

    getContentType:
    17. "returns correct MIME for pdf" — verify 'application/pdf'
    18. "returns correct MIME for jpg/jpeg" — verify 'image/jpeg'
    19. "returns correct MIME for png" — verify 'image/png'
    20. "returns octet-stream for unknown extension" — verify fallback

    Mock mode tests (jest.isolateModules, no SUPABASE_URL):
    21. "mockUploadDocument stores in memory" — verify returns documentId + storagePath
    22. "mockGetDocumentsByUser returns user's documents" — upload 2 docs, verify filtered
    23. "mockGetDocument returns document by ID and userId" — upload then get
    24. "mockGetDocument returns null for wrong userId" — upload as user-1, get as user-2
    25. "mockDeleteDocument removes document" — upload, delete, verify get returns null
    26. "mockDeleteDocument throws for missing document" — delete nonexistent

    Run and verify.
  </action>
  <verify>npx jest __tests__/services/documentService.test.js --verbose --coverage</verify>
  <done>26+ test cases pass, documentService.js >= 90% coverage</done>
</task>
</tasks>

<verification>
- [ ] `npx jest __tests__/services/documentService.test.js` passes all tests
- [ ] `npx jest` (full suite) passes — no regressions
- [ ] Coverage of `services/documentService.js` >= 90%
</verification>
