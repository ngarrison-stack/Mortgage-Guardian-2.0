---
phase: 08-structured-logging
plan: 04
type: plan
---

# 08-04: Logging Tests and Production Verification

<objective>
Add unit tests for the Winston logger utility and verify zero console.* statements remain in production source code.
</objective>

## Context

- `utils/logger.js` created in 08-01
- All 119 console.* statements replaced in 08-02 and 08-03
- Need test coverage for the logger utility itself
- Need a sweep to confirm no console.* leaks

## Tasks

### 1. Create `__tests__/utils/logger.test.js`

Test the logger utility:
- `createLogger(name)` returns a Winston logger instance with correct `service` defaultMeta
- Log levels are correct per NODE_ENV (debug in dev, info in prod)
- `morganStream.write()` delegates to logger.http()
- JSON format in production mode
- Handles errors with stack traces

### 2. Full codebase console.* sweep

- Verify: `grep -rn "console\." --include="*.js" services/ routes/ middleware/ server.js | grep -v node_modules` returns 0
- Document any intentional exceptions (there should be none)

### 3. Run full test suite

- `npm test` — all tests pass (474+)
- Logger tests pass
- No test regressions from logging changes

## Verification

- `npm test` — all tests pass with new logger tests
- Zero console.* in production source (services, routes, middleware, server.js)
- Logger test coverage ≥ 90%

## Files

- `backend-express/__tests__/utils/logger.test.js` — new
