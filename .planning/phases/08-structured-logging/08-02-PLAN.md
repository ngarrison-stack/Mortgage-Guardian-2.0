---
phase: 08-structured-logging
plan: 02
type: plan
---

# 08-02: Replace console.log in Backend Services

<objective>
Replace all console.* statements in service files with structured Winston logging using child loggers — 54 statements across 5 files.
</objective>

## Context

- `utils/logger.js` created in 08-01 with `createLogger(serviceName)` factory
- Service files with console.* counts:
  - `services/plaidDataService.js`: 20 statements
  - `services/plaidService.js`: 16 statements
  - `services/documentService.js`: 12 statements
  - `services/mockPlaidService.js`: 5 statements
  - `services/claudeService.js`: 1 statement

## Tasks

### 1. plaidDataService.js (20 statements)

- `const logger = createLogger('plaid-data');`
- Map console.log → logger.info for operational messages
- Map console.error → logger.error with structured error metadata
- Map console.warn → logger.warn
- Add structured context: `{ userId, accountId, transactionCount }` etc.
- Never log access tokens or secrets — use `[REDACTED]` if referencing

### 2. plaidService.js (16 statements)

- `const logger = createLogger('plaid');`
- Same mapping rules
- API call logging: `logger.info('Plaid API call', { method, endpoint })`
- Error paths: `logger.error('Plaid API error', { error: err.message, code: err.code })`

### 3. documentService.js (12 statements)

- `const logger = createLogger('document');`
- Document operations: `logger.info('Document uploaded', { documentId, userId, fileType })`
- Never log document content or file buffers

### 4. mockPlaidService.js (5 statements)

- `const logger = createLogger('plaid-mock');`
- Mark mock operations clearly: `logger.debug('Mock: returning sandbox data', { method })`

### 5. claudeService.js (1 statement)

- `const logger = createLogger('claude');`
- Replace the single console statement

## Verification

- `npm test` — all 474 tests pass
- `grep -rn "console\." services/ | grep -v node_modules` returns 0 matches
- No sensitive data in log output

## Files

- `backend-express/services/plaidDataService.js` — modified
- `backend-express/services/plaidService.js` — modified
- `backend-express/services/documentService.js` — modified
- `backend-express/services/mockPlaidService.js` — modified
- `backend-express/services/claudeService.js` — modified
