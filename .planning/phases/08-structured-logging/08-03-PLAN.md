---
phase: 08-structured-logging
plan: 03
type: plan
---

# 08-03: Replace console.log in Route Handlers and Middleware

<objective>
Replace all console.* statements in route handlers and middleware with structured Winston logging — 54 statements across 4 files.
</objective>

## Context

- `utils/logger.js` created in 08-01 with `createLogger(serviceName)` factory
- Route/middleware files with console.* counts:
  - `routes/plaid.js`: 45 statements (webhook processing is the heaviest)
  - `routes/documents.js`: 5 statements
  - `routes/claude.js`: 3 statements
  - `middleware/auth.js`: 1 statement

## Tasks

### 1. routes/plaid.js (45 statements)

- `const logger = createLogger('plaid-routes');`
- Webhook processing: `logger.info('Webhook received', { type, itemId })` — structured, not string concatenation
- Transaction sync: `logger.info('Transactions synced', { added, modified, removed })`
- Error paths: `logger.error('Webhook processing failed', { error: err.message, webhookType })`
- Never log request bodies containing tokens

### 2. routes/documents.js (5 statements)

- `const logger = createLogger('document-routes');`
- Upload: `logger.info('Document upload', { userId, fileType, size })`
- Errors: `logger.error('Upload failed', { error: err.message })`

### 3. routes/claude.js (3 statements)

- `const logger = createLogger('claude-routes');`
- Analysis: `logger.info('Analysis requested', { documentType })`
- Errors: `logger.error('Analysis failed', { error: err.message })`

### 4. middleware/auth.js (1 statement)

- `const logger = createLogger('auth');`
- Replace the single console statement

## Verification

- `npm test` — all 474 tests pass
- `grep -rn "console\." routes/ middleware/ | grep -v node_modules` returns 0 matches
- No sensitive data (tokens, keys) in log output

## Files

- `backend-express/routes/plaid.js` — modified
- `backend-express/routes/documents.js` — modified
- `backend-express/routes/claude.js` — modified
- `backend-express/middleware/auth.js` — modified
