---
phase: 23-cicd-pipeline
plan: 03
type: execute
---

<objective>
Clean up the existing iOS CI workflow for correctness, add branch protection documentation, and verify all workflows pass on GitHub.

Purpose: The iOS ci.yml references a standalone .xcodeproj but the complete-ci-cd.yml referenced a .xcworkspace with CocoaPods — need to verify which is correct. Branch protection rules document what status checks to require.
Output: Updated iOS workflow, branch protection guide, all workflows verified green on GitHub.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-cicd-pipeline/23-01-SUMMARY.md
@.planning/phases/23-cicd-pipeline/23-02-SUMMARY.md

# Key files:
@.github/workflows/ci.yml
@.github/workflows/backend-ci.yml
@.github/workflows/frontend-ci.yml

**Constraining decisions:**
- iOS CI uses MortgageGuardian.xcodeproj (standalone, no CocoaPods)
- Backend CI: tests + lint + coverage
- Frontend CI: lint + build
- No deployment automation in this phase (Phases 25-26 scope)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update iOS CI workflow and add branch protection docs</name>
  <files>.github/workflows/ci.yml, .github/BRANCH_PROTECTION.md</files>
  <action>
1. Review `.github/workflows/ci.yml` (iOS CI):
   - Verify it references the correct Xcode project structure (check if MortgageGuardian.xcodeproj exists in repo root or if iOS code is elsewhere)
   - If the iOS project files don't exist in this repo (likely — this is the "Clean" repo), add a conditional check or rename the workflow to clarify it's for the iOS companion app
   - Update action versions to @v4 (upload-artifact currently uses @v4, which is correct)
   - Ensure concurrency group doesn't conflict with backend/frontend workflows (use workflow-specific group name)

2. Create `.github/BRANCH_PROTECTION.md` documenting recommended GitHub branch protection settings:
   - Required status checks: "Backend CI / test", "Frontend CI / build"
   - Optional status check: "iOS CI / Build and Test" (only if iOS code present)
   - Require PR reviews: 1 (recommended)
   - Require branches to be up to date before merging
   - No force pushes to main
   - Include instructions for configuring these in GitHub Settings > Branches > Branch protection rules

This is documentation only — actual branch protection is configured in GitHub UI, not via workflow files.
  </action>
  <verify>All .yml files in .github/workflows/ parse as valid YAML; BRANCH_PROTECTION.md exists</verify>
  <done>iOS workflow reviewed/updated, branch protection documented</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Three CI workflows (backend-ci.yml, frontend-ci.yml, ci.yml) and branch protection documentation</what-built>
  <how-to-verify>
    1. Push changes to a branch: `git push origin HEAD`
    2. Create a PR or check the Actions tab on GitHub
    3. Verify "Backend CI" workflow runs and passes (tests + lint + coverage)
    4. Verify "Frontend CI" workflow runs and passes (lint + build)
    5. Verify "iOS CI" workflow either passes or is appropriately skipped
    6. Check that all three workflows show green checkmarks
    7. Optionally: Configure branch protection per `.github/BRANCH_PROTECTION.md`
  </how-to-verify>
  <resume-signal>Type "approved" if workflows pass on GitHub, or describe any failures to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All workflow YAML files are valid
- [ ] iOS CI workflow is accurate for project structure
- [ ] Branch protection documentation exists
- [ ] User has verified workflows pass on GitHub (checkpoint)
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- All CI workflows pass on GitHub Actions
- Branch protection rules documented
- Phase 23 complete
</success_criteria>

<output>
After completion, create `.planning/phases/23-cicd-pipeline/23-03-SUMMARY.md`
</output>
