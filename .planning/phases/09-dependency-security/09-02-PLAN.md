---
phase: 09-dependency-security
plan: 02
type: plan
---

# 09-02: Update Safe Dependencies (Patch/Minor)

<objective>
Update all dependencies where the update is within the semver-compatible range (patch or minor) — these are safe, non-breaking updates that fix known vulnerabilities.
</objective>

## Context

`npm outdated` shows these safe updates available (Current → Wanted):

### Backend (patch/minor — safe to auto-update)

| Package | Current | Wanted | Type |
|---------|---------|--------|------|
| `@supabase/supabase-js` | 2.80.0 | 2.97.0 | minor |
| `cors` | 2.8.5 | 2.8.6 | patch |
| `dotenv` | 17.2.3 | 17.3.1 | minor |
| `express` | 4.21.2 | 4.22.1 | minor |
| `ioredis` | 5.8.2 | 5.9.3 | minor |
| `joi` | 18.0.1 | 18.0.2 | patch |
| `jsonwebtoken` | 9.0.2 | 9.0.3 | patch |
| `nodemon` | 3.1.10 | 3.1.14 | patch (dev) |
| `winston` | 3.18.3 | 3.19.0 | minor |

### Frontend (patch/minor — safe to auto-update)

| Package | Current | Wanted | Type |
|---------|---------|--------|------|
| `@clerk/nextjs` | 6.34.5 | 6.38.2 | minor |
| `@eslint/eslintrc` | 3.3.1 | 3.3.4 | patch |
| `@tailwindcss/postcss` | 4.1.17 | 4.2.1 | minor |
| `@types/node` | 20.19.24 | 20.19.33 | patch |
| `@types/react` | 19.2.2 | 19.2.14 | patch |
| `@types/react-dom` | 19.2.2 | 19.2.3 | patch |
| `tailwindcss` | 4.1.17 | 4.2.1 | minor |

## Tasks

### 1. Backend: `npm update` in backend-express/

```bash
cd backend-express && npm update
```

This updates all packages to their `Wanted` version (respects semver ranges in package.json).

### 2. Frontend: `npm update` in frontend/

```bash
cd frontend && npm update
```

### 3. Root: Remove or fix root package.json

The root `package.json` just has `plaid` as a dependency with no lock file synced. Either:
- Remove it (plaid is in backend-express already)
- Or `npm install` to sync the lock file

### 4. Verify

- `cd backend-express && npm test` — 488 tests pass
- `cd frontend && npm run build` — builds successfully
- `npm audit` — reduced vulnerability count

## Verification

- Backend: 488 tests pass
- Frontend: builds without errors
- `npm outdated` shows no "Wanted" updates remaining

## Files

- `backend-express/package.json` — version bumps
- `backend-express/package-lock.json` — regenerated
- `frontend/package-lock.json` — regenerated
- `package.json` (root) — possibly cleaned up
