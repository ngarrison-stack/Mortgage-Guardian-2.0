---
phase: 29-security-audit-compliance-review
plan: 01
type: execute
---

<objective>
Fix all npm audit vulnerabilities and restore corrupted server.js file.

Purpose: Establish a clean, vulnerability-free codebase before performing the OWASP security audit.
Output: Zero npm audit vulnerabilities, restored server.js, all tests passing.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Key files:
@backend-express/package.json
@frontend/package.json

# Prior context:
# Phase 28 complete — performance baselines established
# Phase 9 (v2.0) did initial dependency security audit

**Current vulnerabilities:**
- Backend: 4 vulnerabilities (1 critical handlebars, 2 high path-to-regexp/picomatch, 1 moderate brace-expansion) — all fixable via `npm audit fix`
- Frontend: 3 vulnerabilities (1 moderate flatted, 2 high picomatch) — all fixable via `npm audit fix`

**Critical issue:** `backend-express/server.js` has been overwritten with "OAuth token revoked · Please run /login" (40 bytes). The file must be restored from git history. Last known good content is in commit `c3ce5a2` but subsequent commits (through `1148a7c`) also modified it — need to reconstruct the full file incorporating all changes through Phase 28.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore corrupted server.js from git history</name>
  <files>backend-express/server.js</files>
  <action>The file `backend-express/server.js` is corrupted (contains only "OAuth token revoked" text). Reconstruct it by:
  1. Use `git show c3ce5a2:backend-express/server.js` as the base (v4.0 release — last commit where full content is known good)
  2. Apply changes from subsequent commits that modified server.js: `a1cd0de` (env validator integration), `dc2b2a1` (metrics middleware), `93c6f37` (Sentry integration), `1148a7c` (memory profiling)
  3. Use `git diff c3ce5a2..a1cd0de -- backend-express/server.js`, then diff each subsequent pair to reconstruct the delta
  4. Write the fully reconstructed file

  WARNING: Do NOT just use the c3ce5a2 version — it's missing Phase 25-28 changes (env validator, metrics middleware, Sentry, memory profiling). Reconstruct by applying all diffs sequentially, or find the correct content from the commit that introduced the corruption.</action>
  <verify>node -c backend-express/server.js (syntax check passes), file contains helmet/cors/rateLimit/requireAuth/metrics/Sentry/envValidator setup</verify>
  <done>server.js restored with all Phase 18-28 changes intact, syntax valid, grep confirms presence of: helmet, cors, rateLimit, requireAuth, requestId, metrics, Sentry, envValidator</done>
</task>

<task type="auto">
  <name>Task 2: Fix npm audit vulnerabilities in both packages</name>
  <files>backend-express/package.json, backend-express/package-lock.json, frontend/package.json, frontend/package-lock.json</files>
  <action>Run `npm audit fix` in both backend-express/ and frontend/ directories. For each:
  1. Run `npm audit fix` to apply safe fixes
  2. If any remain, check if `npm audit fix --force` is safe (review breaking changes before applying)
  3. Run `npm audit` to verify zero vulnerabilities
  4. If handlebars vulnerability persists (critical): check if it's a direct or transitive dep. If transitive (likely via swagger-ui-express or similar), add overrides in package.json to force safe version.
  
  Do NOT blindly force-update — check semver compatibility. If a fix requires a major version bump that could break things, document it and apply carefully.</action>
  <verify>npm audit --prefix backend-express shows 0 vulnerabilities, npm audit --prefix frontend shows 0 vulnerabilities</verify>
  <done>Both backend and frontend report 0 npm audit vulnerabilities</done>
</task>

<task type="auto">
  <name>Task 3: Verify all tests pass after fixes</name>
  <files>backend-express/package.json</files>
  <action>Run the full backend test suite to confirm nothing was broken by the server.js restoration or dependency updates:
  1. `cd backend-express && npm test` — all 1610+ tests must pass
  2. `cd frontend && npm run build` — frontend must build successfully
  3. If any test failures, diagnose and fix (likely caused by dependency version changes)
  
  Do NOT skip failing tests or mark them as expected failures.</action>
  <verify>npm test --prefix backend-express passes all tests, npm run build --prefix frontend succeeds</verify>
  <done>All 1610+ backend tests pass, frontend builds successfully, zero test failures</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `backend-express/server.js` is valid JavaScript with all Phase 18-28 features
- [ ] `npm audit --prefix backend-express` reports 0 vulnerabilities
- [ ] `npm audit --prefix frontend` reports 0 vulnerabilities
- [ ] `npm test --prefix backend-express` passes all tests
- [ ] `npm run build --prefix frontend` succeeds
</verification>

<success_criteria>

- server.js fully restored with correct content
- Zero npm audit vulnerabilities in both packages
- All tests pass, no regressions
- Ready for OWASP code audit in 29-02
</success_criteria>

<output>
After completion, create `.planning/phases/29-security-audit-compliance-review/29-01-SUMMARY.md`
</output>
