---
phase: 32-express-api-client-migration
plan: 04
type: execute
---

<objective>
Remove AWSBackendClient.swift, purge all remaining AWS Lambda references, and verify the entire iOS project builds cleanly.

Purpose: Complete the migration by removing dead code and verifying the project compiles with Express-only networking.
Output: Clean codebase with no AWS dependencies, successful Xcode build.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-express-api-client-migration/32-01-SUMMARY.md
@.planning/phases/32-express-api-client-migration/32-02-SUMMARY.md
@.planning/phases/32-express-api-client-migration/32-03-SUMMARY.md

**Key files:**
@MortgageGuardian/Services/AWSBackendClient.swift (575 lines — to delete)
@MortgageGuardian/Services/AWSTextractService.swift (references amazonaws.com — assess for deletion)
@MortgageGuardian/Services/CloudMigrationTestNotes.swift (references AWS URL — assess for deletion)

**AWS references to purge (from grep):**
- AWSBackendClient.swift line 41: hardcoded Lambda URL
- AWSTextractService.swift line 870: Textract endpoint URL
- CloudMigrationTestNotes.swift line 43: Lambda URL in notes

**Express backend replaces:**
- AWS Lambda API → Express backend API (via APIClient + APIConfiguration)
- AWS Textract → Local Vision Framework OCR + Express Claude analysis pipeline
- AWS Cognito → Clerk authentication (already in place)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete AWS files and purge remaining references</name>
  <files>MortgageGuardian/Services/AWSBackendClient.swift, MortgageGuardian/Services/AWSTextractService.swift, MortgageGuardian/Services/CloudMigrationTestNotes.swift</files>
  <action>
1. **Delete `AWSBackendClient.swift`** — all functionality migrated to APIClient in Plans 01-03. This file is now dead code.

2. **Assess `AWSTextractService.swift`** — This is an AWS Textract client for server-side OCR. Since the Express backend has its own OCR pipeline (ocrService.js) and the iOS app uses Vision Framework for local OCR:
   - If NO other files import or reference AWSTextractService → DELETE it
   - If other files reference it → check if those references are active or dead code. If dead → delete. If active → stub it out to use Express document processing endpoint instead.
   - Run: `grep -r "AWSTextractService" MortgageGuardian/` to check usage

3. **Delete or update `CloudMigrationTestNotes.swift`** — This is a notes/documentation file with AWS references. If it's just migration notes that are now obsolete → DELETE it. If it contains active test code → update URLs to Express.

4. **Final sweep** — Run `grep -r "amazonaws\|Lambda\|Textract\|AWSBackend\|Cognito" MortgageGuardian/` and eliminate any remaining AWS references (excluding legitimate AWS SDK usage if any exists for other purposes).

5. **Remove from Xcode project** — If the project uses an Xcode project file (.xcodeproj), ensure deleted files are also removed from the project's file references. Run: `grep -r "AWSBackendClient\|AWSTextractService\|CloudMigrationTestNotes" MortgageGuardian.xcodeproj/` and clean up references.

Do NOT delete files that might have non-AWS functionality mixed in. Read them first and assess.
  </action>
  <verify>`grep -r "AWSBackendClient\|h4rj2gpdza\|execute-api.*amazonaws" MortgageGuardian/` returns zero results. `grep -r "AWSTextractService" MortgageGuardian/` returns zero results (if deleted). All deleted files removed from Xcode project file.</verify>
  <done>AWSBackendClient.swift deleted. AWS Lambda/Textract/Cognito references purged from entire iOS codebase. Xcode project file updated.</done>
</task>

<task type="auto">
  <name>Task 2: Xcode build verification and health check test</name>
  <files>MortgageGuardian/Services/APIClient.swift</files>
  <action>
1. **Build the project:**
   ```bash
   xcodebuild -project MortgageGuardian.xcodeproj -scheme MortgageGuardian -destination 'platform=iOS Simulator,name=iPhone 16,OS=latest' build 2>&1 | tail -50
   ```
   Or if it uses a workspace:
   ```bash
   xcodebuild -workspace MortgageGuardian.xcworkspace -scheme MortgageGuardian -destination 'platform=iOS Simulator,name=iPhone 16,OS=latest' build 2>&1 | tail -50
   ```

2. **Fix any compilation errors** — Common issues to expect:
   - Missing type references (if something still references AWSBackendClient types)
   - Import statements that need updating
   - Optional binding changes if error types changed
   - Xcode project file references to deleted files

3. **Add a health check convenience method to APIClient** — This verifies Express connectivity:
   ```swift
   func checkHealth() async throws -> HealthResponse {
       return try await request(
           endpoint: "/health",
           responseType: HealthResponse.self
       )
   }
   ```
   With a simple `HealthResponse` struct: `status`, `version`, `uptime`.
   Note: `/health` is unauthenticated on Express, so this method should work without a token.

4. **Run existing tests** (if any):
   ```bash
   xcodebuild test -project MortgageGuardian.xcodeproj -scheme MortgageGuardian -destination 'platform=iOS Simulator,name=iPhone 16,OS=latest' 2>&1 | tail -100
   ```
   Fix any test failures caused by the migration.

The build MUST succeed. This is the gate for Phase 32 completion.
  </action>
  <verify>`xcodebuild build` exits with code 0 (BUILD SUCCEEDED). `grep -rn "amazonaws" MortgageGuardian/` returns zero results from Swift source files.</verify>
  <done>Xcode project builds successfully with zero errors. Health check method exists. No AWS references remain in Swift source. Phase 32 complete.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `xcodebuild build` succeeds (BUILD SUCCEEDED)
- [ ] Zero AWS Lambda/Textract/Cognito references in Swift source files
- [ ] AWSBackendClient.swift deleted and removed from Xcode project
- [ ] Health check method verifies Express backend connectivity
- [ ] All existing API consumers compile and reference APIClient
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No compilation errors or warnings from migration
- iOS project builds cleanly against Express backend
- Phase 32 complete — all iOS API calls point at Express backend
</success_criteria>

<output>
After completion, create `.planning/phases/32-express-api-client-migration/32-04-SUMMARY.md`
</output>
