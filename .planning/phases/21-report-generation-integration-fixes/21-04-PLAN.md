---
phase: 21-report-generation-integration-fixes
plan: 04
type: execute
---

<objective>
Add end-to-end integrity tests verifying the full report generation pipeline: aggregation → scoring → assembly → schema validation → dispute letter generation from stored reports.

Purpose: Phase 21 fixes multiple handoff points. These tests verify that findings flow correctly from upstream analyses through the consolidated report into dispute letters, and that summary counts match detail sections. They serve as regression protection for the report pipeline.
Output: New test suite covering cross-system data integrity.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-report-generation-integration-fixes/21-CONTEXT.md

# Key files:
@backend-express/services/consolidatedReportService.js
@backend-express/services/reportAggregationService.js
@backend-express/services/confidenceScoringService.js
@backend-express/services/disputeLetterService.js
@backend-express/schemas/consolidatedReportSchema.js
@backend-express/__tests__/services/reportAggregationService.test.js
@backend-express/__tests__/integration/consolidated-report.test.js

# Auto-selected summaries:
@.planning/phases/17-integration-testing-pipeline-hardening/17-04-SUMMARY.md

**Tech stack available:** Jest 29.x, supertest, mock factories from Phase 1
**Established patterns:** Mock caseFileService.getCase, mock Anthropic SDK, test full service methods
**Constraining decisions:**
- [Phase 17-04]: 1205 tests across 47 suites — new tests must not regress existing suite
- [Phase 21-01]: Schema allows null breakdown layers, classificationConfidence wired
- [Phase 21-02]: Dispute letter service reads from both raw and consolidated report formats
- [Phase 21-03]: documentAnalysis preserves anomaly details
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test consolidated report schema validation with real service output</name>
  <files>backend-express/__tests__/services/consolidatedReportService.test.js</files>
  <action>
Create a new test file `backend-express/__tests__/services/consolidatedReportService.test.js` that tests the full generateReport flow with mocked dependencies.

Mock: caseFileService.getCase (return comprehensive case data with documents, forensic analysis, compliance report), Supabase (for persistence), Anthropic SDK (not needed since we skip letter generation).

Tests:
1. **Full report validates against schema** — Call generateReport with a case that has document analyses, forensic report, and compliance report. Run validateConsolidatedReport on the result. Assert valid=true, zero errors.

2. **Partial report validates (no forensic, no compliance)** — Call generateReport with a case that has only document analyses (no forensic_analysis, no compliance_report). Assert valid=true. Assert confidenceScore.breakdown.forensicAnalysis is null, complianceAnalysis is null.

3. **findingSummary counts match detail sections** — From the full report result, verify:
   - findingSummary.byCategory.documentAnomalies === sum of documentAnalysis[].anomalies.length
   - findingSummary.byCategory.crossDocDiscrepancies === forensicFindings.discrepancies.length
   - findingSummary.byCategory.timelineViolations === forensicFindings.timelineViolations.length
   - findingSummary.byCategory.federalViolations === complianceFindings.federalViolations.length
   - findingSummary.byCategory.stateViolations === complianceFindings.stateViolations.length
   - findingSummary.totalFindings === sum of all byCategory values

4. **documentAnalysis preserves anomaly details** — Verify each item in report.documentAnalysis has anomalies array with id, field, type, severity, description fields matching input.

5. **classificationConfidence flows into scoring** — Provide case data where documents have analysis_report.classificationConfidence set. Assert report.confidenceScore.classificationImpact is present with expected factor.

Use the existing mock patterns from reportAggregationService.test.js for fixture construction.
  </action>
  <verify>cd backend-express && npm test -- --testPathPattern="consolidatedReportService" --no-coverage 2>&1 | tail -10</verify>
  <done>5 tests pass covering schema validation, partial data, count consistency, anomaly preservation, and classification confidence pipeline</done>
</task>

<task type="auto">
  <name>Task 2: Test dispute letter generation from stored consolidated report</name>
  <files>backend-express/__tests__/services/disputeLetterService.test.js</files>
  <action>
Create `backend-express/__tests__/services/disputeLetterService.test.js` testing the data extraction methods that were fixed in Plan 21-02.

Mock: Anthropic SDK (mock messages.create to return valid JSON letter response). Set ANTHROPIC_API_KEY env var in test setup.

Tests:
1. **_extractViolations reads from consolidated report format** — Pass a report object with `complianceFindings: { federalViolations: [...], stateViolations: [...] }` (no complianceReport field). Assert violations array includes both federal and state items.

2. **_extractViolations reads from raw aggregated format** — Pass a report object with `complianceReport: { violations: [...], stateViolations: [...] }` (no complianceFindings field). Assert same result.

3. **_extractFindings reads from consolidated report format** — Pass a report with `documentAnalysis: [{ anomalies: [...] }]` (singular, no documentAnalyses) and `forensicFindings: { discrepancies: [...] }`. Assert findings include anomalies and discrepancies.

4. **_extractFindings reads from raw aggregated format** — Pass a report with `documentAnalyses: [...]` (plural) and `forensicReport: { discrepancies: [...] }`. Assert same results.

5. **Full letter generation with stored report** — Pass a realistic stored consolidated report (with complianceFindings, documentAnalysis, forensicFindings). Assert the response has no error, has content object with subject/body/demands, and the Claude prompt included violation details.

Since _extractViolations and _extractFindings are "private" methods, test them indirectly through generateDisputeLetter. For the prompt inspection test, capture the messages.create call arguments and verify the prompt contains violation/finding text.
  </action>
  <verify>cd backend-express && npm test -- --testPathPattern="disputeLetterService" --no-coverage 2>&1 | tail -10</verify>
  <done>5 tests pass covering both data formats for violation/finding extraction and full letter generation from stored reports</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd backend-express && npm test` — all tests pass, zero failures
- [ ] New test files created and passing
- [ ] Tests verify cross-system data integrity
</verification>

<success_criteria>

- Both tasks completed
- All verification checks pass
- 10 new tests covering end-to-end report pipeline integrity
- No regressions in existing test suite
- Tests serve as regression protection for all Phase 21 fixes
</success_criteria>

<output>
After completion, create `.planning/phases/21-report-generation-integration-fixes/21-04-SUMMARY.md`
</output>
