---
phase: 17-integration-testing-pipeline-hardening
plan: 01
type: execute
---

<objective>
Create end-to-end integration tests that exercise the full v3.0 pipeline as a single flow: document intake → OCR → classification → individual analysis → cross-document forensics → federal/state compliance → consolidated report generation.

Purpose: Lock in the full pipeline behavior as a regression safety net. Any future change to Phases 10-16 services that breaks the chain will be caught immediately.
Output: Comprehensive end-to-end pipeline integration test suite covering the happy path and key pipeline state transitions.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-integration-testing-pipeline-hardening/17-CONTEXT.md

# Prior phase context:
@.planning/phases/16-consolidated-findings-reporting/16-05-SUMMARY.md
@.planning/phases/16-consolidated-findings-reporting/16-06-SUMMARY.md

# Key source files — the 4 orchestrators under test:
@backend-express/services/documentPipelineService.js
@backend-express/services/forensicAnalysisService.js
@backend-express/services/complianceService.js
@backend-express/services/consolidatedReportService.js

# Existing test infrastructure:
@backend-express/__tests__/mocks/mockSupabaseClient.js
@backend-express/__tests__/mocks/mockClaudeService.js
@backend-express/__tests__/integration/document-analysis-pipeline.test.js

**Tech stack available:** Jest 29.7, supertest, existing mock factories
**Established patterns:** Hoisted jest.mock, singleton services, mock factory pattern, beforeEach mock reset
**Constraining decisions:**
- Phase 10-04: Best-effort DB persistence; pipeline never blocks on Supabase write failures
- Phase 12-03: Analysis route BEFORE /:documentId prevents Express param matching
- Phase 13-05: Forensic orchestrator uses graceful degradation per step
- Phase 16-05: Report orchestrator Step 1 (GATHER) failure returns error; all other steps degrade
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pipeline-wide mock infrastructure</name>
  <files>backend-express/__tests__/mocks/mockPipelineServices.js</files>
  <action>
Create a centralized mock factory that produces coordinated mock responses across all 4 orchestrators. This factory must return mock data that flows correctly from one stage to the next — i.e., the document IDs from intake appear in forensic analysis results, which appear in compliance findings, which appear in the consolidated report.

The factory should export:
- `createMockPipelineContext()` — returns a complete set of mock data representing a 2-document case (one mortgage statement, one closing disclosure) with:
  - caseId, userId, two documentIds
  - OCR results for each document
  - Classification results (mortgage_statement, closing_disclosure subtypes)
  - Individual analysis results with extractedData, anomalies, completeness scores
  - Cross-document forensic results with 2 discrepancies (amount_mismatch, date_inconsistency)
  - Federal compliance results with 1 RESPA violation
  - State compliance results with 1 CA HBOR violation
  - Consolidated report with confidence score, evidence links, recommendations
  - Dispute letter content

- `setupPipelineMocks(context)` — takes the context and sets up jest.mock for all external boundaries:
  - Anthropic SDK (Claude API) — returns staged responses for classification, analysis, comparison, compliance
  - Supabase client — returns mock case/document data, accepts writes silently
  - Plaid client — returns mock transactions

Use the existing mockSupabaseClient.js chainable pattern. Do NOT create a new Supabase mock pattern — extend/reuse the existing one.

Avoid creating overly complex mock data. Keep each mock response to the minimum fields required by the downstream consumer. Inspect actual service code to determine required fields.
  </action>
  <verify>
Run: `node -e "const m = require('./backend-express/__tests__/mocks/mockPipelineServices'); const ctx = m.createMockPipelineContext(); console.log(Object.keys(ctx));"` — should print all context keys without errors.
  </verify>
  <done>Mock factory exports createMockPipelineContext and setupPipelineMocks, both execute without errors, and mock data contains correct cross-references between pipeline stages.</done>
</task>

<task type="auto">
  <name>Task 2: Create end-to-end pipeline integration tests</name>
  <files>backend-express/__tests__/integration/full-pipeline-e2e.test.js</files>
  <action>
Create an integration test suite that exercises the full document lifecycle across all 4 orchestrators. Use the mock infrastructure from Task 1.

Test cases (minimum):

1. **Full happy path** — Process 2 documents through the complete pipeline:
   - Call documentPipelineService.processDocument() for each document
   - Verify pipeline state transitions: uploaded → ocr → classifying → analyzing → analyzed
   - Call forensicAnalysisService.runAnalysis() with the case
   - Verify cross-document discrepancies are detected
   - Call complianceService.evaluateCompliance() with the case
   - Verify federal and state violations are found
   - Call consolidatedReportService.generateReport() with the case
   - Verify consolidated report contains findings from ALL upstream stages
   - Verify dispute letter is generated when option enabled

2. **Pipeline state consistency** — After full pipeline run:
   - Verify document IDs flow correctly through all stages
   - Verify caseId is consistent across all orchestrator outputs
   - Verify finding counts in consolidated report match upstream totals

3. **Partial pipeline** — Run only intake + analysis (no forensics/compliance):
   - Verify individual analysis works independently
   - Verify consolidated report handles missing forensic/compliance data gracefully (null scores, not errors)

4. **Multi-document scaling** — Process 3+ documents:
   - Verify all documents appear in forensic comparison pairs
   - Verify compliance evaluates all document findings

Mock all external boundaries (Claude API, Supabase, Plaid). The test validates internal orchestration logic, not external API contracts.

Use describe/it blocks organized by pipeline stage. Follow existing test naming conventions (lowercase, descriptive).
  </action>
  <verify>cd backend-express && npm test -- __tests__/integration/full-pipeline-e2e.test.js --verbose 2>&1 | tail -30</verify>
  <done>All end-to-end tests pass. Tests verify data flows correctly across all 4 orchestrators. No regressions in existing 1145 tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd backend-express && npm test -- __tests__/integration/full-pipeline-e2e.test.js` passes all tests
- [ ] `cd backend-express && npm test` — full suite passes with zero regressions
- [ ] End-to-end test exercises all 4 orchestrators in sequence
- [ ] Mock data cross-references are consistent (same IDs flow through pipeline)
</verification>

<success_criteria>

- End-to-end pipeline test suite created and passing
- Mock infrastructure supports coordinated multi-orchestrator testing
- Full test suite (1145+ tests) passes with zero regressions
- Pipeline data flow validated across intake → forensics → compliance → reporting
</success_criteria>

<output>
After completion, create `.planning/phases/17-integration-testing-pipeline-hardening/17-01-SUMMARY.md`
</output>
