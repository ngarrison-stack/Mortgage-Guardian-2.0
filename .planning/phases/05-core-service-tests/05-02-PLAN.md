---
phase: 05-core-service-tests
plan: 02
type: execute
---

<objective>
Write comprehensive unit tests for plaidService.js achieving 90%+ coverage of the Plaid banking integration service.

Purpose: Verify all Plaid API operations (link tokens, token exchange, accounts, transactions, item management, webhooks), helper methods, error formatting, and mock fallback behavior — all with mocked Plaid SDK.
Output: Test file at `__tests__/services/plaidService.test.js` with ~30 test cases.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@backend-express/services/plaidService.js
@backend-express/services/mockPlaidService.js
@backend-express/routes/plaid.js
@backend-express/jest.config.js
</context>

<tasks>
<task type="auto">
  <name>Task 1: Test Plaid API methods with mocked SDK</name>
  <files>backend-express/__tests__/services/plaidService.test.js</files>
  <action>
    Create test file that mocks the 'plaid' module at the module level.

    IMPORTANT: plaidService.js conditionally creates either PlaidService (real) or
    MockPlaidService based on PLAID_CLIENT_ID env var (line ~28-32). It exports a
    singleton. To test the REAL PlaidService class:
    - Set process.env.PLAID_CLIENT_ID and process.env.PLAID_SECRET before requiring
    - Mock the 'plaid' module so PlaidApi constructor returns an object with mock methods

    Mock setup pattern:
    ```
    const mockPlaidClient = {
      linkTokenCreate: jest.fn(),
      itemPublicTokenExchange: jest.fn(),
      accountsGet: jest.fn(),
      transactionsGet: jest.fn(),
      itemGet: jest.fn(),
      itemWebhookUpdate: jest.fn(),
      itemRemove: jest.fn(),
      sandboxPublicTokenCreate: jest.fn()
    };

    jest.mock('plaid', () => ({
      Configuration: jest.fn(),
      PlaidApi: jest.fn(() => mockPlaidClient),
      PlaidEnvironments: { sandbox: 'https://sandbox.plaid.com' },
      Products: { Transactions: 'transactions' },
      CountryCode: { Us: 'US' }
    }));
    ```

    Before requiring plaidService, set env vars:
    ```
    process.env.PLAID_CLIENT_ID = 'test_client_id';
    process.env.PLAID_SECRET = 'test_secret';
    process.env.PLAID_ENV = 'sandbox';
    ```

    Test cases for Plaid API methods:
    1. createLinkToken: success — mock resolves with { data: { link_token, expiration, request_id } }
    2. createLinkToken: forwards userId, clientName, redirectUri parameters
    3. createLinkToken: error — mock rejects, expect throw with formatted error
    4. createSandboxPublicToken: success returns public token
    5. exchangePublicToken: success returns { accessToken, itemId, requestId }
    6. exchangePublicToken: error — rejects with Plaid error format
    7. getAccounts: success returns accounts array
    8. getAccounts: forwards optional account_ids filter
    9. getTransactions: success returns { transactions, totalTransactions, accounts }
    10. getTransactions: forwards startDate, endDate, count, offset parameters
    11. getItem: success returns item details
    12. updateWebhook: success returns updated item with new webhook URL
    13. removeItem: success returns { removed: true, requestId }
    14. Generic API error propagation: Plaid error with type/code/message is thrown
  </action>
  <verify>npx jest __tests__/services/plaidService.test.js --verbose</verify>
  <done>14+ test cases pass, all Plaid API methods covered with success and error paths</done>
</task>

<task type="auto">
  <name>Task 2: Test webhook verification, helpers, testConnection, and mock fallback</name>
  <files>backend-express/__tests__/services/plaidService.test.js</files>
  <action>
    Add test cases to the existing test file:

    verifyWebhookSignature tests (describe block):
    Read plaidService.js to understand the exact signature verification logic before
    writing tests. Then test:
    1. Returns expected result for valid HMAC signature
    2. Returns false for mismatched signature
    3. Handles missing PLAID_WEBHOOK_SECRET env var gracefully

    testConnection tests (describe block):
    4. Returns status object with environment info
    5. Indicates mock mode when using mock service

    formatPlaidError tests (describe block):
    Read plaidService.js to find formatPlaidError implementation. If it's a method
    on PlaidService, test:
    6. Formats Plaid error with type, code, message, displayMessage
    7. Handles non-Plaid error objects (generic Error)
    8. Handles error with missing fields

    isValidDate tests (describe block):
    9. Returns true for 'YYYY-MM-DD' format
    10. Returns false for 'MM/DD/YYYY' (wrong format)
    11. Returns false for empty string
    12. Returns false for null/undefined

    isValidUrl tests (describe block):
    13. Returns true for 'https://example.com/webhook'
    14. Returns false for 'not-a-url'
    15. Returns false for empty/null

    Mock fallback mode tests (describe block):
    Use a SEPARATE describe block with fresh module loading (jest.isolateModules)
    where PLAID_CLIENT_ID is NOT set. Verify:
    16. Service instance is MockPlaidService (or has mock behavior)
    17. createLinkToken returns a mock token string
    18. getAccounts returns mock account data with mortgage-relevant accounts
    19. getTransactions returns filtered mock transactions

    Run full test suite to verify no regressions.
  </action>
  <verify>npx jest __tests__/services/plaidService.test.js --verbose --coverage</verify>
  <done>30+ total test cases pass, 90%+ coverage on plaidService.js, all existing tests still pass</done>
</task>
</tasks>

<verification>
- [ ] `npx jest __tests__/services/plaidService.test.js` passes all tests
- [ ] `npx jest` (full suite) passes — no regressions
- [ ] Coverage of `services/plaidService.js` >= 90%
</verification>

<success_criteria>
- 30+ test cases covering all PlaidService methods, helpers, and mock fallback
- 90%+ coverage on plaidService.js
- No regressions in existing tests
- Both real PlaidService (mocked SDK) and MockPlaidService paths tested
</success_criteria>

<output>
After completion, create `.planning/phases/05-core-service-tests/05-02-SUMMARY.md`
</output>
