---
phase: 05-core-service-tests
plan: 03
type: execute
---

<objective>
Write comprehensive unit tests for financialSecurityService.js covering encryption, credential management, compliance validation, and audit logging.

Purpose: Verify the AWS-backed financial security service's encryption paths, credential lifecycle, compliance checks, fraud detection, and audit chain — all with mocked AWS SDK, Redis, and external dependencies.
Output: Test file at `__tests__/services/financialSecurityService.test.js` with ~25 test cases.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@backend-express/services/financialSecurityService.js
@backend-express/jest.config.js
</context>

<tasks>
<task type="auto">
  <name>Task 1: Mock infrastructure and test encryption/credential methods</name>
  <files>backend-express/__tests__/services/financialSecurityService.test.js</files>
  <action>
    Create test file with comprehensive module-level mocking.

    CRITICAL: financialSecurityService.js exports a singleton (line 849:
    `module.exports = new FinancialSecurityService()`). The constructor calls
    initializeRateLimiters() (needs Redis), initializeEncryption() (needs AWS),
    and initializeComplianceChecks() (pure config). ALL external dependencies
    must be mocked BEFORE the module is required.

    Mock setup (all via jest.mock() at top of file):
    1. 'ioredis' — mock constructor returns object with get/set/incr/expire/sadd/
       sismember/del as jest.fn()
    2. 'aws-sdk' — mock KMS (encrypt/decrypt/sign returning .promise()),
       SecretsManager (createSecret/updateSecret/getSecretValue returning .promise()),
       CloudHSMV2 (describeClusters returning .promise())
    3. 'rate-limiter-flexible' — mock RateLimiterRedis constructor, return object
       with consume as jest.fn().mockResolvedValue(true)
    4. 'jsonwebtoken' — mock verify to return decoded payload
    5. 'speakeasy' — mock totp.verify to return true
    6. 'winston' — mock createLogger returning { info: jest.fn(), error: jest.fn() }
    7. 'winston-elasticsearch' — mock ElasticsearchTransport constructor

    Set required env vars before requiring:
    - process.env.KMS_KEY_ID = 'test-key-id'
    - process.env.KMS_SIGNING_KEY_ID = 'test-signing-key'
    - process.env.JWT_SECRET = 'test-jwt-secret'

    Test cases:
    encryptWithKMS / decryptWithKMS:
    1. "encryptWithKMS returns base64 ciphertext" — mock KMS.encrypt resolves,
       verify params include KeyId and EncryptionContext
    2. "decryptWithKMS returns plaintext" — mock KMS.decrypt resolves,
       verify CiphertextBlob passed correctly
    3. "encryptWithHSM falls back to KMS when HSM not available" — hsmClient
       is null (default), verify KMS.encrypt called

    storeCredential:
    4. "stores encrypted credential in Secrets Manager" — mock encrypt + createSecret,
       verify both called in order
    5. "updates existing secret on ResourceExistsException" — mock createSecret to
       throw { code: 'ResourceExistsException' }, verify updateSecret called
    6. "consumes rate limit before storing" — verify rateLimiters.credential.consume called
    7. "creates audit log entries for attempt and success" — verify auditLog called twice

    retrieveCredential:
    8. "retrieves and decrypts credential" — mock getSecretValue + decrypt, verify
       returns decrypted value
    9. "calls validateZeroTrust before retrieval" — verify validateZeroTrust called
    10. "throws on rate limit exceeded" — mock consume to reject, expect error

    rotateCredential:
    11. "generates new credential and stores it" — verify generateSecureCredential
        and storeCredential called

    generateSecureCredential:
    12. "returns base64 string of 32 random bytes" — verify output is valid base64
        and decoded length is 32 bytes
  </action>
  <verify>npx jest __tests__/services/financialSecurityService.test.js --verbose</verify>
  <done>12 test cases pass, encryption and credential management paths verified</done>
</task>

<task type="auto">
  <name>Task 2: Test compliance, fraud detection, audit, helpers, and middleware</name>
  <files>backend-express/__tests__/services/financialSecurityService.test.js</files>
  <action>
    Add test cases to the existing test file:

    validateCompliance tests (describe block):
    1. "throws for PCI DSS violation — untokenized payment" — call with
       ('paymentProcessing', { tokenized: false, encrypted: true }), expect throw
       containing 'PCI_DSS'
    2. "throws for SOC2 violation — missing audit trail" — call with any operation
       and { auditTrail: false }, expect throw containing 'SOC2'
    3. "throws for GLBA violation — data sharing without consent" — call with
       ('dataSharing', { customerConsent: false }), expect throw
    4. "passes when all compliance requirements met" — call with valid context,
       expect returns true

    detectFraud tests (describe block):
    5. "returns ALLOW for low-risk transaction" — transaction with amount: 100,
       mock low velocity, expect riskScore < 0.3 and action 'ALLOW'
    6. "returns REVIEW for medium-risk transaction" — transaction with amount: 15000
       (HIGH_VALUE_TRANSACTION), mock normal velocity
    7. "returns BLOCK for high-risk transaction" — mock high velocity + risky location +
       high ML score, expect action 'BLOCK'
    8. "creates audit log for every transaction" — verify auditLog called with
       'TRANSACTION_RISK_ASSESSMENT'

    auditLog tests (describe block):
    9. "creates entry with UUID, timestamp, hash chain" — verify entry has id,
       timestamp, hash, previousHash
    10. "calculates SHA-512 hash of entry content" — verify calculateHash produces
        consistent hash for same input
    11. "signs entry with KMS" — verify KMS.sign called with hash

    sanitizeForLog tests (describe block):
    12. "truncates string to 4 chars + ****" — call with 'secret123',
        expect 'secr****'
    13. "returns **** for non-string values" — call with number/object

    securityMiddleware tests (describe block):
    14. "sets security headers" — create mock req/res/next, verify res.setHeader
        called for X-Content-Type-Options, X-Frame-Options, X-XSS-Protection, etc.
    15. "returns 403 on security validation failure" — mock validateZeroTrust to
        throw, verify res.status(403)

    Run full test suite to verify no regressions.
  </action>
  <verify>npx jest __tests__/services/financialSecurityService.test.js --verbose --coverage</verify>
  <done>27 total test cases pass, 90%+ coverage on financialSecurityService.js, all existing tests still pass</done>
</task>
</tasks>

<verification>
- [ ] `npx jest __tests__/services/financialSecurityService.test.js` passes all tests
- [ ] `npx jest` (full suite) passes — no regressions
- [ ] Coverage of `services/financialSecurityService.js` >= 90%
</verification>

<success_criteria>
- 25+ test cases covering encryption, credentials, compliance, fraud, audit, and middleware
- 90%+ coverage on financialSecurityService.js
- No regressions in existing tests
- All AWS, Redis, and external dependencies properly mocked
</success_criteria>

<output>
After completion, create `.planning/phases/05-core-service-tests/05-03-SUMMARY.md`
</output>
