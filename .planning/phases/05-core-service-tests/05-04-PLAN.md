---
phase: 05-core-service-tests
plan: 04
type: execute
---

<objective>
Write comprehensive unit tests for vendorNeutralSecurityService.js covering the platform-agnostic encryption, secret management, session management, and audit logging classes.

Purpose: Verify the vendor-neutral security implementations that use native Node.js crypto (no AWS dependency), including NativeEncryptionProvider, FilesystemSecretManager, InMemorySessionManager, ImmutableAuditLog, ZeroKnowledgeAuth, and createSecurityMiddleware.
Output: Test file at `__tests__/services/vendorNeutralSecurityService.test.js` with ~25 test cases.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@backend-express/services/vendorNeutralSecurityService.js
@backend-express/jest.config.js
</context>

<tasks>
<task type="auto">
  <name>Task 1: Test NativeEncryptionProvider, secret managers, and session managers</name>
  <files>backend-express/__tests__/services/vendorNeutralSecurityService.test.js</files>
  <action>
    Create test file importing specific classes from vendorNeutralSecurityService.js.

    NOTE: Unlike financialSecurityService, vendorNeutralSecurityService exports
    individual classes (not a singleton). This makes testing much cleaner — instantiate
    classes directly with test configuration.

    Mock setup:
    1. 'fs' (promises) — mock readFile, writeFile, mkdir, readdir, unlink, appendFile
       Use jest.mock('fs', () => ({ promises: { ... } }))
    2. 'ioredis' — mock for RedisSessionManager tests
    3. 'rate-limiter-flexible' — mock for VendorNeutralSecurityService rate limiter
    4. 'winston' — mock createLogger
    5. 'winston-syslog' and 'winston-elasticsearch' — mock if referenced

    NativeEncryptionProvider tests (describe block):
    Mock fs.readFileSync to throw ENOENT (forces key generation) and
    fs.writeFileSync/mkdirSync as jest.fn(). Use jest.spyOn(crypto, 'randomBytes')
    if needed, but prefer testing the actual crypto behavior:

    1. "encrypt returns base64 string" — encrypt('hello'), verify result is valid base64
    2. "decrypt reverses encrypt (round-trip)" — encrypt then decrypt, verify
       original plaintext returned
    3. "encrypt produces different output for same input" — two encrypts of same
       plaintext produce different ciphertexts (due to random IV)
    4. "decrypt rejects tampered ciphertext" — modify a byte in encrypted output,
       expect decrypt to throw (auth tag mismatch)
    5. "generateKey returns base64 string of 32 bytes" — verify output decodes to 32 bytes
    6. "deriveKey produces consistent output for same password+salt" — two calls with
       same params return same derived key

    FilesystemSecretManager tests (describe block):
    7. "setSecret encrypts and writes to file" — mock writeFile, verify called with
       encrypted content at correct path
    8. "getSecret reads and decrypts from file" — mock readFile to return encrypted
       data, verify decrypted result
    9. "getSecret returns null for missing secret" — mock readFile to throw ENOENT
    10. "deleteSecret removes file" — mock unlink, verify called with correct path
    11. "listSecrets returns secret names from .enc files" — mock readdir

    EnvironmentSecretManager tests (describe block):
    12. "getSecret returns env var value" — set process.env.TEST_KEY, verify getSecret
    13. "getSecret returns null for unset key"

    InMemorySessionManager tests (describe block):
    14. "createSession returns unique session ID" — verify returned ID is hex string
    15. "getSession returns session data" — create then get, verify userId and metadata
    16. "getSession updates lastActivity" — create, wait, get, verify lastActivity updated
    17. "deleteSession removes session" — create, delete, get returns null
    18. "cleanup removes expired sessions" — create session, manually set lastActivity
        to past (>15 min), call cleanup(), verify session gone
    19. "destroy clears all sessions" — create multiple, destroy, verify all gone
  </action>
  <verify>npx jest __tests__/services/vendorNeutralSecurityService.test.js --verbose</verify>
  <done>19 test cases pass, encryption, secret management, and session management verified</done>
</task>

<task type="auto">
  <name>Task 2: Test ImmutableAuditLog, ZeroKnowledgeAuth, and createSecurityMiddleware</name>
  <files>backend-express/__tests__/services/vendorNeutralSecurityService.test.js</files>
  <action>
    Add test cases to the existing test file:

    ImmutableAuditLog tests (describe block):
    Mock fs.promises (mkdir, readFile, appendFile) for all audit log tests.

    1. "logEntry creates entry with UUID, timestamp, and hash" — call logEntry,
       verify returned entry has id (UUID format), timestamp (ISO), and hash (hex)
    2. "logEntry chains hashes — previousHash matches prior entry's hash" — log two
       entries, verify second entry's previousHash equals first entry's hash
    3. "calculateHash produces consistent SHA-512 for same input" — call twice
       with identical entry, verify same hash
    4. "calculateHash produces different hashes for different input" — two different
       entries produce different hashes

    ZeroKnowledgeAuth tests (describe block):
    5. "registerUser stores salt and verifier (not password)" — register user,
       verify internal storage has salt and verifier, NOT the original password
    6. "beginAuthentication returns salt and B value" — register then begin auth,
       verify response has salt (hex) and B (hex)
    7. "beginAuthentication throws for unknown user" — call without registering,
       expect 'User not found' error
    8. "verifyAuthentication validates session" — register, begin, verify with proof

    createSecurityMiddleware tests (describe block):
    Create mock req, res, next objects for Express middleware testing:
    ```
    const mockReq = { ip: '127.0.0.1', method: 'GET', path: '/test',
                      connection: { remoteAddress: '127.0.0.1' },
                      headers: { 'user-agent': 'test' } };
    const mockRes = { setHeader: jest.fn(), status: jest.fn().mockReturnThis(),
                      json: jest.fn() };
    const mockNext = jest.fn();
    ```

    9. "sets security headers (X-Content-Type-Options, X-Frame-Options, etc.)" —
       create middleware, call it, verify res.setHeader called with correct headers
    10. "calls next() on success" — verify mockNext called
    11. "returns 429 on rate limit exceeded" — mock rateLimiter.api.consume to reject
        with { name: 'RateLimiterError', msBeforeNext: 60000 }, verify
        res.status(429).json called with retryAfter

    VendorNeutralSecurityService tests (describe block):
    12. "constructor initializes components based on config" — instantiate with
        default config, verify encryptionProvider and sessionManager exist
    13. "createEncryptionProvider returns NativeEncryptionProvider for 'native'" —
        verify instanceof NativeEncryptionProvider

    Run full test suite to verify no regressions.
  </action>
  <verify>npx jest __tests__/services/vendorNeutralSecurityService.test.js --verbose --coverage</verify>
  <done>32 total test cases pass, 90%+ coverage on vendorNeutralSecurityService.js, all existing tests still pass</done>
</task>
</tasks>

<verification>
- [ ] `npx jest __tests__/services/vendorNeutralSecurityService.test.js` passes all tests
- [ ] `npx jest` (full suite) passes — no regressions
- [ ] Coverage of `services/vendorNeutralSecurityService.js` >= 90%
</verification>

<success_criteria>
- 30+ test cases covering encryption providers, secret managers, sessions, audit, auth, and middleware
- 90%+ coverage on vendorNeutralSecurityService.js
- No regressions in existing tests
- NativeEncryptionProvider round-trip encryption verified with real crypto
</success_criteria>

<output>
After completion, create `.planning/phases/05-core-service-tests/05-04-SUMMARY.md`
</output>
