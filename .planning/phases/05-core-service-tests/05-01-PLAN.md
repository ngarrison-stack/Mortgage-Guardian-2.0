---
phase: 05-core-service-tests
plan: 01
type: execute
---

<objective>
Write comprehensive unit tests for claudeService.js achieving 90%+ coverage of the Claude AI integration service.

Purpose: Verify analyzeDocument API calls, prompt building for all mortgage document types, testConnection, and error handling — all with mocked Anthropic SDK.
Output: Test file at `__tests__/services/claudeService.test.js` with ~15 test cases.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@backend-express/services/claudeService.js
@backend-express/routes/claude.js
@backend-express/jest.config.js
@backend-express/__tests__/utils/fileValidation.test.js
</context>

<tasks>
<task type="auto">
  <name>Task 1: Test analyzeDocument with mocked Anthropic SDK</name>
  <files>backend-express/__tests__/services/claudeService.test.js</files>
  <action>
    Create test file that mocks @anthropic-ai/sdk at the module level.

    IMPORTANT: claudeService.js creates the Anthropic client at module scope (line 4)
    and exports a singleton (line 141). The mock MUST be set up via jest.mock() before
    requiring claudeService — Jest hoists jest.mock() calls automatically.

    Mock setup:
    - jest.mock('@anthropic-ai/sdk') at top of file
    - In beforeEach: configure the mock constructor to return an object with
      messages.create as a jest.fn()
    - Get reference to the mock via: const Anthropic = require('@anthropic-ai/sdk')

    Test cases for analyzeDocument (describe block):
    1. "returns formatted response on success" — mock messages.create to resolve with
       { content: [{ text: 'analysis' }], model: 'claude-3-5-sonnet-20241022',
         usage: { input_tokens: 100, output_tokens: 50 }, stop_reason: 'end_turn' }
       Assert returns { content, model, usage: { inputTokens, outputTokens }, stopReason }
    2. "forwards model, maxTokens, temperature parameters" — verify messages.create
       called with { model: 'custom-model', max_tokens: 1000, temperature: 0.5, ... }
    3. "uses default model when not specified" — call without model, verify
       messages.create received model: 'claude-3-5-sonnet-20241022'
    4. "uses default maxTokens=4096 and temperature=0.1" — call without those params,
       verify messages.create received max_tokens: 4096, temperature: 0.1
    5. "throws on 401 authentication error" — mock rejects with { status: 401 },
       expect the promise to reject
    6. "throws on 429 rate limit error" — mock rejects with { status: 429 },
       expect the promise to reject
    7. "throws on network error" — mock rejects with generic Error,
       expect the promise to reject
    8. "passes prompt as user message content" — verify messages.create receives
       messages: [{ role: 'user', content: prompt }]

    Pattern: Use beforeEach to clear and reconfigure mocks.
  </action>
  <verify>npx jest __tests__/services/claudeService.test.js --verbose</verify>
  <done>8 test cases pass, analyzeDocument method fully covered including error paths</done>
</task>

<task type="auto">
  <name>Task 2: Test buildMortgageAnalysisPrompt and testConnection</name>
  <files>backend-express/__tests__/services/claudeService.test.js</files>
  <action>
    Add test cases to the existing test file:

    buildMortgageAnalysisPrompt tests (describe block):
    1. "returns mortgage_statement prompt containing document text" — call with
       ('sample text', 'mortgage_statement'), assert result contains 'sample text'
       and contains 'mortgage auditor'
    2. "returns escrow_statement prompt" — call with ('text', 'escrow_statement'),
       assert contains 'escrow' keywords
    3. "returns payment_history prompt" — call with ('text', 'payment_history'),
       assert contains 'payment history' keywords
    4. "returns default prompt for unknown type" — call with ('text', 'unknown_type'),
       assert contains 'document analyst'
    5. "returns default prompt when documentType is undefined" — call with ('text'),
       assert same as default
    6. "embeds documentText in all prompt variants" — verify for each type that
       the provided documentText appears in the returned string

    testConnection tests (describe block):
    7. "returns success with message when API responds" — mock analyzeDocument
       (via messages.create) to resolve, assert { success: true, message: '...' }
    8. "returns failure with error when API throws" — mock to reject,
       assert { success: false, error: '...' }
    9. "calls analyzeDocument with maxTokens: 20" — verify messages.create
       called with max_tokens: 20

    Run full test suite to confirm no regressions.
  </action>
  <verify>npx jest __tests__/services/claudeService.test.js --verbose --coverage</verify>
  <done>17 total test cases pass, 90%+ coverage on claudeService.js, all existing tests still pass</done>
</task>
</tasks>

<verification>
- [ ] `npx jest __tests__/services/claudeService.test.js` passes all tests
- [ ] `npx jest` (full suite) passes — no regressions
- [ ] Coverage of `services/claudeService.js` >= 90% statements, branches, functions, lines
</verification>

<success_criteria>
- 15+ test cases covering analyzeDocument, buildMortgageAnalysisPrompt, and testConnection
- 90%+ coverage on claudeService.js
- No regressions in existing 169 tests
- Mock setup correctly isolates from real Anthropic API
</success_criteria>

<output>
After completion, create `.planning/phases/05-core-service-tests/05-01-SUMMARY.md`
</output>
