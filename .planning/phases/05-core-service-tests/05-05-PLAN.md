---
phase: 05-core-service-tests
plan: 05
type: execute
---

<objective>
Write comprehensive unit tests for plaidDataService.js and cross-service integration tests verifying service interactions and error propagation.

Purpose: Verify the Plaid data persistence layer (Supabase operations + mock mode) and test that services interact correctly when composed — Claude analysis feeding document storage, Plaid data flowing through webhook handlers.
Output: Two test files: `__tests__/services/plaidDataService.test.js` (~15 tests) and `__tests__/services/service-integration.test.js` (~10 tests).
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@backend-express/services/plaidDataService.js
@backend-express/services/claudeService.js
@backend-express/services/plaidService.js
@backend-express/services/documentService.js
@backend-express/jest.config.js
</context>

<tasks>
<task type="auto">
  <name>Task 1: Test plaidDataService with mocked Supabase</name>
  <files>backend-express/__tests__/services/plaidDataService.test.js</files>
  <action>
    Create test file that mocks @supabase/supabase-js.

    IMPORTANT: plaidDataService.js creates a Supabase client at module scope (lines 7-13)
    and exports a singleton (line 336). Mock Supabase BEFORE requiring.

    Mock setup:
    ```
    const mockSupabase = {
      from: jest.fn().mockReturnThis(),
      upsert: jest.fn().mockReturnThis(),
      insert: jest.fn().mockReturnThis(),
      select: jest.fn().mockReturnThis(),
      update: jest.fn().mockReturnThis(),
      delete: jest.fn().mockReturnThis(),
      eq: jest.fn().mockReturnThis(),
      in: jest.fn().mockReturnThis(),
      single: jest.fn()
    };

    jest.mock('@supabase/supabase-js', () => ({
      createClient: jest.fn(() => mockSupabase)
    }));
    ```

    Set env vars:
    - process.env.SUPABASE_URL = 'https://test.supabase.co'
    - process.env.SUPABASE_SERVICE_KEY = 'test-service-key'

    Test cases for Supabase mode:
    upsertPlaidItem:
    1. "upserts item data to plaid_items table" — mock resolves, verify from('plaid_items')
       and upsert called with correct fields (item_id, user_id, access_token, status)
    2. "returns { success: false } on database error" — mock upsert to return
       { error: { message: 'DB error' } }, verify error handling

    storeTransactions:
    3. "bulk upserts transactions to plaid_transactions" — verify upsert called with
       array of transaction records, onConflict: 'transaction_id'
    4. "maps Plaid transaction fields to database columns" — verify field mapping
       (transaction_id, account_id, amount, category as JSON, location as JSON)

    removeTransactions:
    5. "deletes transactions by IDs" — verify delete().in('transaction_id', ids) called

    updateItemStatus:
    6. "updates status and error fields" — verify update called with status, error, updated_at

    upsertAccounts:
    7. "upserts account records with balance data" — verify field mapping including
       balances.current, balances.available

    getItem:
    8. "returns item data by item_id" — mock single() to return item data

    createNotification:
    9. "inserts notification record" — verify insert with user_id, type, message, priority

    Mock mode tests (describe block using jest.isolateModules):
    Clear SUPABASE_URL and SUPABASE_SERVICE_KEY, re-require module:
    10. "mockUpsertItem stores in memory Map" — call, verify returns { success: true, mock: true }
    11. "mockStoreTransactions stores transactions" — call, verify count returned
    12. "mockGetItem returns stored item" — upsert then get, verify data matches
    13. "mockRemoveTransactions removes by ID" — store then remove, verify

    Run full suite.
  </action>
  <verify>npx jest __tests__/services/plaidDataService.test.js --verbose --coverage</verify>
  <done>13+ test cases pass, both Supabase and mock modes covered</done>
</task>

<task type="auto">
  <name>Task 2: Cross-service integration tests</name>
  <files>backend-express/__tests__/services/service-integration.test.js</files>
  <action>
    Create integration test file that verifies service interactions.

    These tests mock external APIs but test real service-to-service interactions
    within the backend. Focus on the data flow paths visible in the route handlers.

    Mock setup:
    - Mock @anthropic-ai/sdk (for Claude service)
    - Mock plaid SDK (for Plaid service)
    - Mock @supabase/supabase-js (for data services)

    Test cases:

    Claude → Document flow:
    1. "Claude analysis result can be stored via documentService" — call
       claudeService.analyzeDocument with mocked response, take the result,
       pass it as analysisResults to documentService.uploadDocument (mock mode),
       verify document stored with analysis data
    2. "Claude API error does not corrupt document storage" — mock Claude to throw,
       verify documentService still works independently

    Plaid → PlaidData flow:
    3. "exchangePublicToken result feeds into plaidDataService.upsertPlaidItem" —
       mock Plaid SDK exchange, pass result to plaidDataService (mock mode),
       verify item stored with correct accessToken and itemId
    4. "getTransactions results feed into plaidDataService.storeTransactions" —
       mock Plaid SDK transactions response, pass to storeTransactions,
       verify transaction count matches

    Error propagation:
    5. "Plaid API error propagates with type and code" — mock Plaid to throw
       { type: 'INVALID_REQUEST', code: 'INVALID_ACCESS_TOKEN' }, verify the error
       reaches the caller with those fields intact
    6. "Claude API error preserves status code" — mock Anthropic to throw
       { status: 429, message: 'rate limited' }, verify caller receives the error
       with status intact
    7. "Supabase error in plaidDataService returns { success: false }" — mock
       Supabase to return error, verify plaidDataService method returns failure
       object rather than throwing (graceful degradation pattern)

    Service isolation:
    8. "Claude service failure does not affect Plaid service" — mock Claude to fail
       persistently, verify Plaid operations still succeed
    9. "Each service maintains independent state" — verify documentService mock
       documents are separate from plaidDataService mock items

    Run full test suite.
  </action>
  <verify>npx jest __tests__/services/service-integration.test.js --verbose && npx jest --coverage</verify>
  <done>9+ integration test cases pass, cross-service data flows verified, all existing tests still pass</done>
</task>
</tasks>

<verification>
- [ ] `npx jest __tests__/services/plaidDataService.test.js` passes all tests
- [ ] `npx jest __tests__/services/service-integration.test.js` passes all tests
- [ ] `npx jest` (full suite) passes — no regressions
- [ ] Coverage of `services/plaidDataService.js` >= 90%
</verification>

<success_criteria>
- 22+ test cases across both files
- plaidDataService.js at 90%+ coverage (both Supabase and mock modes)
- Cross-service data flows verified end-to-end
- Error propagation patterns documented through tests
- No regressions in existing tests
</success_criteria>

<output>
After completion, create `.planning/phases/05-core-service-tests/05-05-SUMMARY.md`
</output>
