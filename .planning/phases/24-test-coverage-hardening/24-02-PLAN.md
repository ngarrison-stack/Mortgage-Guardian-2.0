---
phase: 24-test-coverage-hardening
plan: 02
type: execute
---

<objective>
Add test coverage for Claude and Documents routes — the two remaining route files below 90%.

Purpose: `routes/claude.js` is at 58.6% and `routes/documents.js` at 60.5%. Together with plaid.js (Plan 01), these three files are why the routes/ layer is at 58.1% overall. Closing these gaps brings routes/ near the 90% target.
Output: New or extended route test files for claude and documents routes with full endpoint coverage.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Key files:
@backend-express/routes/claude.js
@backend-express/routes/documents.js
@backend-express/__tests__/routes/documents-routes.test.js
@backend-express/__tests__/routes/cases-routes.test.js
@backend-express/__tests__/mocks/mockSupabaseClient.js

**Established patterns:** supertest integration tests, module cache reset, hoisted mock functions
**Current coverage:**
- routes/claude.js: 58.62% Stmts, 40% Branch — uncovered lines 18, 40-57, 64-81
- routes/documents.js: 60.55% Stmts, 46.42% Branch — uncovered lines 68-69, 77-81, 152-153, 214-232, 239-249, 255-277, 284-301
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Claude route tests to cover uncovered paths</name>
  <files>backend-express/__tests__/routes/claude-routes.test.js</files>
  <action>Read routes/claude.js to understand the uncovered lines (18, 40-57, 64-81). These are likely error handling paths and alternative request scenarios.

Create or extend claude route tests:
1. If no claude-routes.test.js exists, create one following the established supertest pattern
2. Mock claudeService (analyzeDocument, etc.)
3. Cover the uncovered branches:
   - Line 18: likely an auth/validation check
   - Lines 40-57: likely an error handling path or alternative analysis flow
   - Lines 64-81: likely another endpoint or error case
4. Test both success and error responses for each endpoint
5. Test request validation (missing fields, invalid format)

Since claude.js is only 85 lines, 2-3 additional test cases should close the gap significantly.</action>
  <verify>cd backend-express && npx jest __tests__/routes/claude-routes.test.js --verbose --coverage 2>&1 | grep -A2 "claude.js"</verify>
  <done>routes/claude.js coverage ≥90% statements and ≥80% branches</done>
</task>

<task type="auto">
  <name>Task 2: Extend documents route tests to cover uncovered paths</name>
  <files>backend-express/__tests__/routes/documents-routes.test.js</files>
  <action>Read routes/documents.js to understand the uncovered lines. The existing documents-routes.test.js covers some paths but misses ~40% of the file.

Extend the existing test file:
1. Read both documents.js and documents-routes.test.js to understand current coverage gaps
2. Identify untested endpoints and branches from the uncovered line ranges:
   - Lines 68-81: likely a specific route handler or validation
   - Lines 152-153: error branch
   - Lines 214-301: large block — likely document processing, status endpoints, or bulk operations
3. Add test cases for each uncovered path:
   - Success responses
   - Validation failures (missing fields, invalid IDs)
   - Service error propagation (mock service throws, route returns 500)
   - Auth failures (missing/invalid token)
4. Focus on branch coverage — test both sides of each conditional

Do NOT duplicate existing tests. Read the test file first to understand what's already covered.</action>
  <verify>cd backend-express && npx jest __tests__/routes/documents-routes.test.js --verbose --coverage 2>&1 | grep -A2 "documents.js"</verify>
  <done>routes/documents.js coverage ≥85% statements and ≥75% branches</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd backend-express && npx jest --coverage 2>&1 | grep "routes/"` — overall routes/ coverage ≥80%
- [ ] `cd backend-express && npm test` — all tests pass, no regressions
- [ ] routes/claude.js ≥90% statements
- [ ] routes/documents.js ≥85% statements
</verification>

<success_criteria>
- Claude routes ≥90% statements, ≥80% branches
- Documents routes ≥85% statements, ≥75% branches
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/24-test-coverage-hardening/24-02-SUMMARY.md`
</output>
