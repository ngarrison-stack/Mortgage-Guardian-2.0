---
phase: 24-test-coverage-hardening
plan: 04
type: execute
---

<objective>
Close coverage gaps in documentPipelineService.js and plaidCrossReferenceService.js — the two remaining services below 90% statements.

Purpose: documentPipelineService orchestrates the entire document processing flow and plaidCrossReferenceService handles bank data cross-referencing. Both are critical path services with uncovered error handling and branch logic.
Output: Extended tests for pipeline and cross-reference services bringing them to 90%+ coverage.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Key files:
@backend-express/services/documentPipelineService.js
@backend-express/services/plaidCrossReferenceService.js
@backend-express/__tests__/services/documentPipeline-integration.test.js
@backend-express/__tests__/services/plaidCrossReferenceService.test.js

**Current coverage:**
- documentPipelineService.js: 85.71% Stmts, 76.52% Branch, 72.41% Funcs — uncovered lines 77, 87-97, 118-120, 174, 207-238, 288-296, 485, 523, 527, 531, 650-655, 657
- plaidCrossReferenceService.js: 87.65% Stmts, 63.63% Branch, 90.62% Funcs — uncovered lines 78-82, 175, 196-198, 354, 375-385
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend documentPipelineService tests for uncovered functions and branches</name>
  <files>backend-express/__tests__/services/documentPipeline-integration.test.js</files>
  <action>Read documentPipelineService.js to understand the uncovered paths. At 72.41% function coverage, several functions are completely untested.

Extend the existing integration test:
1. Read both source and test to understand current coverage
2. Identify the untested functions (72.41% = ~8 of 29 functions untested)
3. Key uncovered areas:
   - Lines 87-97: likely an initialization or config function
   - Lines 207-238: substantial block — likely a pipeline stage handler
   - Lines 288-296: error recovery or fallback logic
   - Lines 650-657: likely cleanup or finalization
4. Add tests for each uncovered function:
   - Mock upstream services (OCR, Claude, classification)
   - Test pipeline stage transitions
   - Test error cases within pipeline (one stage fails, pipeline handles gracefully)
   - Test cleanup/finalization paths

Focus on function coverage first (72.41% → 90%+), then branches.</action>
  <verify>cd backend-express && npx jest __tests__/services/documentPipeline-integration.test.js --verbose --coverage 2>&1 | grep -A2 "documentPipelineService"</verify>
  <done>documentPipelineService.js coverage ≥90% statements, ≥80% branches, ≥85% functions</done>
</task>

<task type="auto">
  <name>Task 2: Improve plaidCrossReferenceService branch coverage</name>
  <files>backend-express/__tests__/services/plaidCrossReferenceService.test.js</files>
  <action>Read plaidCrossReferenceService.js to understand uncovered branches. Statement coverage is 87.65% but branch coverage is only 63.63%.

Extend the existing test file:
1. Read source to identify conditional branches at uncovered lines
2. Key gaps:
   - Lines 78-82: likely a validation or guard clause
   - Lines 196-198: error/edge case
   - Lines 375-385: likely a comparison or matching logic branch
3. Add targeted branch tests:
   - Cross-reference with no matching transactions → specific path
   - Cross-reference with partial matches → boundary conditions
   - Null/missing bank data fields
   - Amount threshold edge cases (exact matches, off-by-one)
4. Each test should exercise one specific branch path

Focus exclusively on branch coverage — the 63.63% to 80%+ improvement.</action>
  <verify>cd backend-express && npx jest __tests__/services/plaidCrossReferenceService.test.js --verbose --coverage 2>&1 | grep -A2 "plaidCrossReference"</verify>
  <done>plaidCrossReferenceService.js coverage ≥90% statements, ≥80% branches</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] documentPipelineService.js ≥90% statements, ≥85% functions
- [ ] plaidCrossReferenceService.js ≥90% statements, ≥80% branches
- [ ] `cd backend-express && npm test` — all tests pass
</verification>

<success_criteria>
- Both services meet 90%+ statement coverage targets
- Branch coverage improved significantly (pipeline ≥80%, cross-ref ≥80%)
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/24-test-coverage-hardening/24-04-SUMMARY.md`
</output>
