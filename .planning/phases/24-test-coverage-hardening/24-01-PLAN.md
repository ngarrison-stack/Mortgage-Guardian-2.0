---
phase: 24-test-coverage-hardening
plan: 01
type: execute
---

<objective>
Add comprehensive test coverage for the Plaid routes file — the single largest coverage gap at 9.8% statements across 769 lines.

Purpose: The `routes/plaid.js` file handles all Plaid banking integration endpoints (link tokens, token exchange, accounts, transactions, webhooks, institution search). At 9.8% coverage with 0% branch coverage, this is the most critical gap blocking the 90% target.
Output: Plaid route tests covering all endpoints with success/error paths, raising routes/ coverage significantly.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Key files:
@backend-express/routes/plaid.js
@backend-express/__tests__/routes/cases-routes.test.js
@backend-express/__tests__/routes/documents-routes.test.js
@backend-express/__tests__/mocks/mockSupabaseClient.js
@backend-express/__tests__/services/plaidService.test.js

**Tech stack available:** Jest 29.7.0, supertest, ts-jest
**Established patterns:** Route integration tests use supertest with module cache reset, mock external SDKs at module level, use mockSupabaseClient factory
**Constraining decisions:**
- Phase 23: GitHub Actions billing lock prevents CI runs but workflows are structurally correct
- Testing patterns established in v2.0 (Phases 1-9): __tests__/ directory structure, hoisted mocking, chainable Supabase mock

**Current coverage for routes/plaid.js:** 9.82% Stmts, 0% Branch, 0% Funcs, 9.82% Lines
**Lines uncovered:** 24-37, 64-85, 114-145, 167-176, 199-222, 246-255, 272-281, 297-306, 331-665, 687-709, 725-730, 743-762
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Plaid routes test file — link token, exchange, accounts, transactions</name>
  <files>backend-express/__tests__/routes/plaid-routes.test.js</files>
  <action>Create integration test file for Plaid routes using the established pattern from documents-routes.test.js and cases-routes.test.js:

1. Hoist mock functions for plaidService methods (createLinkToken, exchangePublicToken, getAccounts, getTransactions)
2. Mock `../services/plaidService` module
3. Mock `@supabase/supabase-js` using mockSupabaseClient factory
4. Mock auth middleware to inject userId
5. Use `require('supertest')` with module cache reset in beforeAll

Test these endpoints:
- `POST /v1/plaid/create_link_token` — success returns link_token, missing userId returns 400
- `POST /v1/plaid/exchange_public_token` — success returns access_token, missing public_token returns 400
- `GET /v1/plaid/accounts/:accessToken` — success returns accounts array, service error returns 500
- `GET /v1/plaid/transactions/:accessToken` — success returns transactions with default dates, custom date params work, service error returns 500

Follow existing patterns: beforeEach resets all mocks, each test has single assertion focus, use descriptive test names.</action>
  <verify>cd backend-express && npx jest __tests__/routes/plaid-routes.test.js --verbose 2>&1 | tail -30</verify>
  <done>All Plaid core endpoint tests pass — link token, exchange, accounts, transactions covered with success and error paths</done>
</task>

<task type="auto">
  <name>Task 2: Add Plaid webhook, institution search, and remaining endpoint tests</name>
  <files>backend-express/__tests__/routes/plaid-routes.test.js</files>
  <action>Extend the plaid-routes test file to cover remaining endpoints:

1. Read routes/plaid.js to identify all remaining untested endpoints (webhooks, institution search, balance, investment endpoints, etc.)
2. Add test suites for each:
   - Webhook endpoint: valid signature processes successfully, invalid signature returns 401
   - Institution search: returns results, handles empty query
   - Any remaining GET/POST endpoints
3. Add error/edge case tests for branch coverage:
   - Invalid access token format
   - Plaid API timeout/error scenarios
   - Missing required fields on each POST endpoint

Ensure mock setup covers all service methods called by the remaining routes. Use jest.fn() with mockResolvedValue/mockRejectedValue for async service methods.</action>
  <verify>cd backend-express && npx jest __tests__/routes/plaid-routes.test.js --verbose --coverage 2>&1 | grep -A5 "plaid.js"</verify>
  <done>routes/plaid.js coverage reaches ≥85% statements and ≥70% branches</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd backend-express && npx jest --coverage 2>&1 | grep "routes/plaid"` shows ≥85% statements
- [ ] `cd backend-express && npm test` — all 1275+ tests still pass, no regressions
- [ ] New plaid-routes.test.js follows established patterns (supertest, mock factories, module cache reset)
</verification>

<success_criteria>
- routes/plaid.js coverage ≥85% statements, ≥70% branches
- All existing tests still pass
- No new test flakiness introduced
</success_criteria>

<output>
After completion, create `.planning/phases/24-test-coverage-hardening/24-01-SUMMARY.md`
</output>
