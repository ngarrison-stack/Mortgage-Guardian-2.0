---
phase: 24-test-coverage-hardening
plan: 05
type: execute
---

<objective>
Close remaining coverage gaps in utilities, financial security internals, and verify the 90% global target is met.

Purpose: Several smaller files are below 90% (logger.js at 78.6%, financialSecurity/config.js at 77.3%, financialSecurity/index.js at 75%). Plus global branch coverage sits at 74.4% and needs lifting. This final plan closes the remaining gaps and verifies the overall 90% target.
Output: Tests for remaining undercovered files, plus a verified coverage report meeting the 90% target.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Key files:
@backend-express/utils/logger.js
@backend-express/services/financialSecurity/config.js
@backend-express/services/financialSecurity/index.js
@backend-express/services/financialSecurity/credentials.js

**Current coverage gaps (remaining small files):**
- utils/logger.js: 78.57% Stmts, 60% Branch — uncovered lines 31-33
- services/financialSecurity/config.js: 77.27% Stmts, 77.27% Branch, 0% Funcs
- services/financialSecurity/index.js: 75% Stmts, 16.66% Branch
- services/financialSecurity/credentials.js: 100% Stmts, 44.44% Branch (branches only)

**Global target:** 90% statements, 90% branches, 90% functions, 90% lines
**Current global:** 87.61% Stmts, 74.45% Branch, 90.07% Funcs, 88.17% Lines
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tests for logger, financialSecurity config/index, and remaining branch gaps</name>
  <files>backend-express/__tests__/utils/logger.test.js, backend-express/__tests__/services/financialSecurityService.test.js</files>
  <action>Close the remaining small file coverage gaps:

1. **logger.js** (lines 31-33): Read the file to understand uncovered code. Likely the non-test environment logger setup or a fallback. Add a test that covers the production logger path (temporarily set NODE_ENV, require logger, verify transport configuration).

2. **financialSecurity/config.js** (0% functions, 77% stmts): Read the file. It likely exports configuration functions that are never directly called in tests. Add direct unit tests for the config functions — test with different env vars or config inputs.

3. **financialSecurity/index.js** (16.66% branch): Read the file. The index likely has conditional logic for choosing providers or initializing the service. Test the different initialization paths.

4. **financialSecurity/credentials.js** (44.44% branch): Add tests for the untested conditional branches — likely around different credential storage backends or fallback logic.

For each: read the source first, identify exact uncovered lines, write targeted tests. Extend existing test files where they exist, create new ones only if needed.</action>
  <verify>cd backend-express && npx jest --coverage 2>&1 | grep -E "(logger|financialSecurity)" | head -10</verify>
  <done>All four files reach ≥90% statements and ≥80% branches</done>
</task>

<task type="auto">
  <name>Task 2: Run full coverage report, identify any remaining gaps, and verify 90% target</name>
  <files>backend-express/jest.config.js</files>
  <action>Run the full coverage report and analyze:

1. Run `npm run test:coverage` and capture full output
2. Check global totals against 90% target for all four metrics (statements, branches, functions, lines)
3. If any metric is below 90%:
   - Identify the top 2-3 files dragging it down
   - Add targeted tests to close the gap
   - Focus on branch coverage (currently 74.45%) which is the hardest metric
4. If branch coverage cannot reach 90% without unreasonable effort (testing deeply nested error handlers that would require complex mocking), document the remaining gaps with justification
5. Verify jest.config.js coverage thresholds match the target (ensure coverageThreshold is set to 90%)
6. Final verification: `npm test` passes with no failures

Do NOT modify source code to improve coverage — only add/extend tests. Do NOT mark code as istanbul-ignore unless it's genuinely untestable (e.g., catch blocks for impossible errors).</action>
  <verify>cd backend-express && npx jest --coverage 2>&1 | grep "All files" | head -1</verify>
  <done>Global coverage ≥90% statements, ≥85% branches (branch target relaxed from 90% if deeply nested error handlers remain), ≥90% functions, ≥90% lines. All tests pass.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd backend-express && npm run test:coverage` — global statements ≥90%
- [ ] Global branches ≥85% (document any remaining gaps if <90%)
- [ ] Global functions ≥90%, lines ≥90%
- [ ] All tests pass with zero failures
- [ ] No source code modified (tests only)
</verification>

<success_criteria>
- Global statement coverage ≥90%
- Global branch coverage ≥85% (with documented justification for any gaps below 90%)
- Global function and line coverage ≥90%
- All 1275+ tests still pass
- Phase 24 complete
</success_criteria>

<output>
After completion, create `.planning/phases/24-test-coverage-hardening/24-05-SUMMARY.md`
</output>
