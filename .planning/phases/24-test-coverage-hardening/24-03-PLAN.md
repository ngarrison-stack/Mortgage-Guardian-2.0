---
phase: 24-test-coverage-hardening
plan: 03
type: execute
---

<objective>
Close coverage gaps in compliance services — complianceAnalysisService.js (63.4%) and complianceRuleEngine.js (87.6% stmts but 66.8% branches).

Purpose: These two services implement the core compliance checking logic. Low branch coverage means edge cases in regulatory rule matching are untested — risky for a mortgage compliance platform.
Output: Extended compliance service tests covering uncovered branches and error paths.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Key files:
@backend-express/services/complianceAnalysisService.js
@backend-express/services/complianceRuleEngine.js
@backend-express/__tests__/services/service-integration.test.js

**Current coverage:**
- complianceAnalysisService.js: 63.38% Stmts, 51.07% Branch, 62.5% Funcs — uncovered lines 39, 104-109, 132, 168-245, 404-419, 468-522
- complianceRuleEngine.js: 87.64% Stmts, 66.8% Branch — uncovered lines 400, 412, 508-509, 595-611, 653-705

**Established patterns:** Service unit tests mock external dependencies, test at method level with describe blocks per function
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add complianceAnalysisService tests for uncovered functions and branches</name>
  <files>backend-express/__tests__/services/complianceAnalysisService.test.js</files>
  <action>Read complianceAnalysisService.js to understand the uncovered code paths (lines 39, 104-109, 132, 168-245, 404-419, 468-522).

Create or extend the test file:
1. Check if complianceAnalysisService.test.js exists — if so, extend it; if not, create it
2. Mock dependencies (Supabase, other services called by compliance analysis)
3. Cover the large uncovered block at lines 168-245 — this is likely a major analysis function
4. Cover lines 404-419 and 468-522 — likely error handling or secondary analysis paths
5. Test with various document types and compliance scenarios:
   - Document with clear violations → returns findings
   - Clean document → returns no violations
   - Malformed input → appropriate error handling
   - Edge cases for branch coverage (null fields, empty arrays, missing optional data)

Focus on the 62.5% function coverage — at least 2-3 functions are completely untested.</action>
  <verify>cd backend-express && npx jest __tests__/services/complianceAnalysisService.test.js --verbose --coverage 2>&1 | grep -A2 "complianceAnalysis"</verify>
  <done>complianceAnalysisService.js coverage ≥85% statements, ≥75% branches, ≥90% functions</done>
</task>

<task type="auto">
  <name>Task 2: Improve complianceRuleEngine branch coverage</name>
  <files>backend-express/__tests__/services/complianceRuleEngine.test.js</files>
  <action>Read complianceRuleEngine.js to understand uncovered branches at lines 400, 412, 508-509, 595-611, 653-705. Statement coverage is decent (87.6%) but branch coverage is 66.8% — many conditionals only tested on one path.

Extend the existing test file (check if it exists first):
1. Identify untested conditional branches in the uncovered line ranges
2. Lines 595-611 and 653-705 are substantial blocks — likely rule evaluation paths for specific compliance scenarios or edge cases
3. Add tests that exercise the "else" branches:
   - Rules with boundary conditions (exact threshold matches)
   - Rules that don't match (negative cases)
   - Null/undefined optional fields in rule configuration
   - State-specific rules (if jurisdiction-dependent branches exist)
4. Ensure both paths of each if/else are tested

The goal is branch coverage improvement — write targeted tests for specific conditionals, not broad integration tests.</action>
  <verify>cd backend-express && npx jest __tests__/services/complianceRuleEngine.test.js --verbose --coverage 2>&1 | grep -A2 "complianceRuleEngine"</verify>
  <done>complianceRuleEngine.js coverage ≥90% statements, ≥80% branches</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] complianceAnalysisService.js ≥85% statements, ≥75% branches
- [ ] complianceRuleEngine.js ≥90% statements, ≥80% branches
- [ ] `cd backend-express && npm test` — all tests pass
</verification>

<success_criteria>
- Both compliance services meet or exceed branch coverage targets
- All existing tests pass
- New tests follow established patterns
</success_criteria>

<output>
After completion, create `.planning/phases/24-test-coverage-hardening/24-03-SUMMARY.md`
</output>
