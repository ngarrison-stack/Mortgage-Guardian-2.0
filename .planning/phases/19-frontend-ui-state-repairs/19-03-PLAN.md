---
phase: 19-frontend-ui-state-repairs
plan: 03
type: execute
---

<objective>
Fix layout bugs, metadata, CSS conflicts, and replace the default boilerplate page with proper Mortgage Guardian branding.

Purpose: The frontend currently shows a generic Next.js starter page with wrong metadata and broken CSS classes — it needs to present as the Mortgage Guardian application.
Output: Properly branded layout, correct metadata, working CSS, and a functional landing page.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-frontend-ui-state-repairs/19-01-SUMMARY.md
@.planning/phases/19-frontend-ui-state-repairs/19-02-SUMMARY.md

**Bugs to fix:**
1. `layout.tsx` metadata: title is "Clerk Next.js Quickstart", description is "Generated by create next app" — should be "Mortgage Guardian"
2. `layout.tsx` uses `text-ceramic-white` class which is not a standard Tailwind class and likely renders as unstyled text
3. `globals.css` sets `font-family: Arial, Helvetica, sans-serif` on body, which overrides the Geist font variables set in layout.tsx
4. `page.tsx` is the default create-next-app boilerplate showing Next.js logo, "Deploy now" button, Vercel links — none of this is Mortgage Guardian content

**Branding reference:** The `website/index.html` marketing site uses:
- Color scheme: #2997FF (blue), #30D158 (green), #FF453A (red), #FF9F0A (orange)
- Font: DM Sans
- Dark theme (black background, light text)
- Shield/checkmark logo SVG

**Key files:**
@frontend/src/app/layout.tsx
@frontend/src/app/page.tsx
@frontend/src/app/globals.css
@website/index.html (for branding reference only — do not modify)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix metadata, layout CSS, and font conflict</name>
  <files>frontend/src/app/layout.tsx, frontend/src/app/globals.css</files>
  <action>
1. **Fix metadata in layout.tsx:**
   - Change title from "Clerk Next.js Quickstart" to "Mortgage Guardian — AI-Powered Mortgage Audit"
   - Change description from "Generated by create next app" to "AI-powered forensic audit that detects mortgage servicer violations and generates legal documents."

2. **Fix broken CSS class in layout.tsx:**
   - Replace `text-ceramic-white` with `text-white` (standard Tailwind class) in the SignUpButton styling

3. **Fix font conflict in globals.css:**
   - Remove `font-family: Arial, Helvetica, sans-serif;` from the body rule — the Geist fonts are already loaded via `next/font/google` in layout.tsx and applied via CSS variables `--font-geist-sans` and `--font-geist-mono`
   - The `@theme inline` block already maps `--font-sans` to `--font-geist-sans`, so Tailwind's `font-sans` utility will use Geist

4. **Update header styling** — The header in layout.tsx is minimal but functional. Update it to:
   - Add "Mortgage Guardian" text/branding next to the auth buttons
   - Use a more appropriate background (e.g., `bg-gray-950` or transparent) to match the dark theme from the marketing site
   - Keep the Clerk SignIn/SignUp/UserButton components — they work correctly

Do NOT change the ClerkProvider setup or middleware — authentication is working correctly.
  </action>
  <verify>cd frontend && npm run build succeeds, grep "ceramic-white" frontend/src/ returns no results, grep "Clerk Next.js Quickstart" frontend/src/ returns no results</verify>
  <done>Metadata shows "Mortgage Guardian", no broken CSS classes, fonts render correctly without conflict</done>
</task>

<task type="auto">
  <name>Task 2: Replace default boilerplate page with Mortgage Guardian landing</name>
  <files>frontend/src/app/page.tsx</files>
  <action>
Replace the default create-next-app content in page.tsx with a simple Mortgage Guardian landing page. This is a bug fix (removing incorrect content), not a new feature.

The page should:
1. Show the Mortgage Guardian shield logo (inline SVG matching the marketing site's shield+checkmark)
2. Display "Mortgage Guardian" heading and a brief tagline
3. For signed-out users: Show a call-to-action pointing to sign-in (Clerk handles this)
4. For signed-in users: Show a simple dashboard placeholder with:
   - "Welcome to Mortgage Guardian" heading
   - Brief description of what the platform does
   - Status card showing "Document Analysis: Ready" / "Bank Verification: Ready" / "Compliance Engine: Ready" — reflecting the backend services that are operational
5. Use Tailwind classes for styling — dark theme consistent with the marketing site colors (bg-gray-950, text-gray-100, blue accents #2997FF)

Import `SignedIn` and `SignedOut` from `@clerk/nextjs` to conditionally render content.

Do NOT:
- Add API calls or data fetching (that's new feature work)
- Install new dependencies (use only Tailwind + Clerk)
- Create complex component hierarchies — keep it in a single page file
- Remove the Next.js Image component import if still needed, otherwise clean up unused imports
  </action>
  <verify>cd frontend && npm run build succeeds, page.tsx no longer references "next.svg" or "vercel.svg" or "Deploy now"</verify>
  <done>Page shows Mortgage Guardian branding instead of Next.js boilerplate, different content for signed-in vs signed-out users</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Fixed frontend layout, metadata, CSS, and landing page</what-built>
  <how-to-verify>
    1. Run: cd frontend && npm run dev
    2. Visit: http://localhost:3000
    3. Verify: Page shows "Mortgage Guardian" branding, not Next.js boilerplate
    4. Verify: Browser tab title says "Mortgage Guardian — AI-Powered Mortgage Audit"
    5. Verify: Header has Mortgage Guardian branding with Sign In/Sign Up buttons
    6. Verify: No broken CSS (no unstyled text, fonts look correct, dark theme)
    7. Verify: No console errors in browser dev tools
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `npm run build` succeeds
- [ ] `npm run lint` passes
- [ ] No references to "Clerk Next.js Quickstart" or "create next app"
- [ ] No references to next.svg, vercel.svg, or "Deploy now"
- [ ] No broken CSS classes (ceramic-white)
- [ ] No font conflicts (Arial vs Geist)
- [ ] Page renders Mortgage Guardian content
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Frontend presents as Mortgage Guardian, not a starter template
- Build and lint pass
- Phase 19 complete
</success_criteria>

<output>
After completion, create `.planning/phases/19-frontend-ui-state-repairs/19-03-SUMMARY.md`
</output>
