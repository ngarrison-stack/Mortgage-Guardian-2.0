---
phase: 22-database-migration-framework
plan: 03
type: execute
---

<objective>
Verify the migration framework is complete and reproducible.

Purpose: Prove that the baseline migration captures everything the codebase needs, and that a fresh database can be stood up from the migration files alone.
Output: Verified, complete migration framework — Phase 22 complete.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/22-database-migration-framework/22-CONTEXT.md
@.planning/phases/22-database-migration-framework/22-01-SUMMARY.md
@.planning/phases/22-database-migration-framework/22-02-SUMMARY.md

# Baseline migration (from Plan 22-02):
@supabase/migrations/ (baseline file)

# Services that reference database tables:
@backend-express/services/documentService.js
@backend-express/services/caseFileService.js
@backend-express/services/plaidDataService.js
@backend-express/services/documentPipelineService.js

**Known tables from code:**
- documents, case_files, plaid_items, plaid_accounts, plaid_transactions, notifications, pipeline_state, document_classifications

**Essential from CONTEXT.md:**
- Completeness — everything captured (tables, indexes, RLS, functions, triggers)
- Reproducibility — identical database from scratch every time
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cross-reference migration completeness against codebase</name>
  <files>(read-only analysis)</files>
  <action>
    Systematically verify the baseline migration captures everything the codebase needs:

    **1. Table completeness:**
    Grep all `.from('tablename')` calls across backend-express/services/ and routes/.
    For each table found, verify it exists in the baseline migration's CREATE TABLE statements.
    Report any missing tables.

    **2. Column completeness:**
    For each service file, grep for column names used in `.select()`, `.insert()`, `.update()`, `.eq()`, `.order()` calls.
    Spot-check that these columns exist in the corresponding CREATE TABLE statement in the baseline.
    Focus on the `documents` table (since it had no original DDL in old migrations).

    **3. RLS policy completeness:**
    Count RLS policies in baseline migration.
    Compare against policies in old archived migrations (001-005).
    Report any policies in old migrations missing from baseline.

    **4. Index completeness:**
    Count indexes in baseline migration.
    Compare against indexes in old archived migrations.
    Report any missing indexes.

    **5. Function/trigger completeness:**
    Verify `update_updated_at_column()` function exists.
    Verify all triggers reference existing functions.

    If any gaps found, fix them in the baseline migration file directly.
  </action>
  <verify>
    - All 8 tables accounted for in baseline
    - No column references in code point to missing columns (spot-check report)
    - RLS policy count matches or exceeds old migrations
    - Index count matches or exceeds old migrations
    - All functions and triggers present
    - Report generated listing verification results
  </verify>
  <done>Completeness verified — all tables, columns (spot-checked), RLS policies, indexes, functions, and triggers confirmed in baseline migration</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Supabase CLI migration framework with baseline migration, rollback support, and verification</what-built>
  <how-to-verify>
    Verify the migration framework works end-to-end:

    1. **Check migration files exist:**
       ```
       ls supabase/migrations/
       ```
       Should show the baseline migration file.

    2. **Check supabase status:**
       ```
       supabase status
       ```
       Should show linked project.

    3. **Optional — Test local reset (requires Docker):**
       If Docker is running, test a clean database setup:
       ```
       supabase db reset
       ```
       This applies all migrations from scratch on a local Supabase instance.
       Verify it completes without errors.

    4. **Review migration file:**
       Open the baseline migration in `supabase/migrations/` and confirm:
       - All tables are present
       - RLS policies look correct
       - It reads like a faithful capture of the existing schema

    5. **Check archived migrations:**
       ```
       ls backend-express/migrations/_archived/
       ```
       Should show the 5 original migration files + README.md

    Confirm: Migration framework is complete and captures the full database schema.
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 22, or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] All tables referenced in code exist in baseline migration
- [ ] RLS policies complete
- [ ] Indexes complete
- [ ] Functions and triggers complete
- [ ] Migration framework documented (supabase/README.md)
- [ ] Old migrations archived
- [ ] User has verified the setup
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Migration framework captures complete database schema
- Reproducibility confirmed (or documented for user to test with Docker)
- Phase 22 complete — ready for Phase 23 (CI/CD Pipeline)
</success_criteria>

<output>
After completion, create `.planning/phases/22-database-migration-framework/22-03-SUMMARY.md`

Update `.planning/STATE.md`:
- Current position: Phase 22, Plan 3 of 3
- Status: Phase complete

Update `.planning/ROADMAP.md`:
- Mark Phase 22 plans as complete
</output>
