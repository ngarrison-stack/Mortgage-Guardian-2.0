---
phase: 22-database-migration-framework
plan: 01
type: execute
---

<objective>
Set up Supabase CLI and capture the complete remote database schema.

Purpose: Get a full, authoritative snapshot of the production database — tables, indexes, RLS policies, functions, triggers, storage policies — as the foundation for version-controlled migrations.
Output: Complete schema dump file capturing everything in the remote Supabase database.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-database-migration-framework/22-CONTEXT.md

# Existing manual migrations (capture-as-is reference):
@backend-express/migrations/001_plaid_tables.sql
@backend-express/migrations/002_case_files_and_classifications.sql
@backend-express/migrations/003_pipeline_state.sql
@backend-express/migrations/004_document_rls_policies.sql
@backend-express/migrations/005_storage_bucket_policies.sql

# Services that reference database tables (completeness check):
@backend-express/services/documentService.js
@backend-express/services/caseFileService.js
@backend-express/services/plaidDataService.js
@backend-express/services/documentPipelineService.js

**Supabase project ref:** huosfjdcnjzdzhkkjaqh

**Known tables from code:**
- documents (documentService, caseFileService)
- case_files (caseFileService)
- plaid_items (plaidDataService)
- plaid_accounts (plaidDataService)
- plaid_transactions (plaidDataService)
- notifications (plaidDataService)
- pipeline_state (documentPipelineService)
- document_classifications (migration 002)

**Key gap:** The `documents` table DDL is not in any existing migration — it was created manually or by Supabase bootstrapping. The dump will capture it.
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Supabase CLI initialization</what-built>
  <how-to-verify>
    The following commands should have been run (or need to be run now):

    1. Verify Supabase CLI is installed: `supabase --version`
    2. Verify logged in: `supabase projects list` (should show your projects)
    3. Check if `supabase/` directory exists in project root
    4. If NOT initialized yet, run from project root:
       - `supabase init`
       - `supabase link --project-ref huosfjdcnjzdzhkkjaqh`
    5. Verify link: `supabase status` (should show linked project)

    Confirm: supabase/ directory exists and project is linked.
  </how-to-verify>
  <resume-signal>Type "approved" when Supabase CLI is initialized and linked, or describe any issues</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Dump complete remote schema</name>
  <files>supabase/schema_dump.sql</files>
  <action>
    Run `supabase db dump` from the project root to capture the full remote schema. This pulls everything: tables, columns, constraints, indexes, RLS policies, functions, triggers, storage policies, and extensions.

    Save the raw dump to `supabase/schema_dump.sql` for reference.

    After dumping, verify completeness by checking the dump contains all 8 known tables:
    - documents, case_files, plaid_items, plaid_accounts, plaid_transactions, notifications, pipeline_state, document_classifications

    Also check for:
    - RLS policies (grep for "CREATE POLICY")
    - Trigger functions (grep for "CREATE.*FUNCTION")
    - Indexes (grep for "CREATE INDEX")
    - Storage policies (grep for "storage")

    If `supabase db dump` fails with auth errors, stop and report — the user may need to re-authenticate.

    Do NOT modify the dump — this is the raw capture. Migration organization happens in Plan 22-02.
  </action>
  <verify>
    - `supabase/schema_dump.sql` exists and is non-empty
    - All 8 tables present in dump (grep for CREATE TABLE or table names)
    - RLS policies present (grep for "CREATE POLICY" or "ROW LEVEL SECURITY")
    - Trigger functions present (grep for "update_updated_at_column")
    - Report any tables found in dump that are NOT in the known list (unexpected tables)
  </verify>
  <done>Complete schema dump saved, all 8 known tables confirmed present, any unexpected objects documented</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Supabase CLI initialized and linked to project huosfjdcnjzdzhkkjaqh
- [ ] `supabase/schema_dump.sql` contains complete schema
- [ ] All 8 known tables present in dump
- [ ] RLS policies, functions, triggers, indexes captured
- [ ] Any unexpected objects noted for Plan 22-02
</verification>

<success_criteria>

- Supabase CLI is set up and linked
- Full schema dump captured from remote database
- Completeness verified against known table list
- Ready for Plan 22-02 (migration organization)
</success_criteria>

<output>
After completion, create `.planning/phases/22-database-migration-framework/22-01-SUMMARY.md`
</output>
