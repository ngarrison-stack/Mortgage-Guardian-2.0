---
phase: 03-input-validation-framework
plan: 05
type: execute
domain: testing
---

<objective>
Create comprehensive tests for the Joi validation middleware and all endpoint schemas, verifying valid inputs pass, invalid inputs fail with correct error messages, and edge cases are handled.

Purpose: Ensure validation layer is robust and prevents malformed input from reaching service handlers.
Output: Test files in `backend-express/__tests__/` covering validate middleware + all schemas, achieving 90%+ coverage on validation code.
</objective>

<context>
@.planning/PROJECT.md
@backend-express/middleware/validate.js (created in 03-01)
@backend-express/schemas/documents.js (created in 03-02)
@backend-express/schemas/plaid.js (created in 03-03)
@backend-express/schemas/claude.js (created in 03-04)
@backend-express/routes/documents.js (modified in 03-02)
@backend-express/routes/plaid.js (modified in 03-03)
@backend-express/routes/claude.js (modified in 03-04)
@backend-express/__tests__/middleware/auth.test.js (testing pattern reference)
@backend-express/__tests__/routes/auth-integration.test.js (integration test pattern reference)
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create validate middleware unit tests</name>
  <files>backend-express/__tests__/middleware/validate.test.js</files>
  <action>
    Create unit tests for the validate middleware factory:

    1. Test that validate(schema) returns Express middleware function
    2. Test valid body input: middleware calls next() and replaces req.body with validated value
    3. Test invalid body input: middleware returns 400 with { error: 'Bad Request', message: '...' }
    4. Test stripUnknown: extra fields are removed from req.body
    5. Test abortEarly: false — multiple validation errors reported in single message
    6. Test type coercion: string "123" coerced to number 123 when schema expects number
    7. Test query source: validate(schema, 'query') validates req.query instead of req.body
    8. Test params source: validate(schema, 'params') validates req.params

    Use mock req/res/next objects (same pattern as auth.test.js).
    Import Joi directly to create test schemas — do NOT import endpoint-specific schemas here.
  </action>
  <verify>`cd backend-express && npx jest __tests__/middleware/validate.test.js --verbose` — all tests pass</verify>
  <done>Unit tests cover validate middleware: valid input, invalid input, stripUnknown, abortEarly, type coercion, and all sources (body/query/params).</done>
</task>

<task type="auto">
  <name>Task 2: Create schema validation integration tests</name>
  <files>backend-express/__tests__/validation/schemas.test.js</files>
  <action>
    Create integration tests that validate schemas against realistic endpoint inputs:

    **Document schemas:**
    - uploadDocumentSchema: valid complete body, valid minimal body (required fields only), missing required fields, extra fields stripped
    - getDocumentsSchema: valid query with all params, valid with defaults, invalid limit (>500), invalid offset (<0)
    - getDocumentSchema: valid userId, missing userId
    - deleteDocumentSchema: valid userId, missing userId

    **Plaid schemas:**
    - linkTokenSchema: valid complete body, valid minimal (user_id only), user_id too long (>255), products not array
    - exchangeTokenSchema: valid public token, invalid format (not starting with 'public-')
    - accountsSchema: valid access token (access- and access_sandbox-), invalid format
    - transactionsSchema: valid complete body, invalid date format, count out of range, negative offset
    - itemSchema: valid, invalid format
    - updateWebhookSchema: valid with https, valid with http, invalid URL
    - deleteItemSchema: valid, invalid format
    - sandboxTokenSchema: valid complete, valid empty (all optional)

    **Claude schemas:**
    - analyzeSchema: valid with prompt only, valid with documentText only, valid with both, missing both (should fail with or() error), maxTokens out of range, temperature out of range

    Test pattern: Call `schema.validate(input)` directly — no Express or supertest needed.
    Assert on `.error` (null for valid, Joi ValidationError for invalid) and `.value` (coerced values).
  </action>
  <verify>`cd backend-express && npx jest __tests__/validation/schemas.test.js --verbose` — all tests pass</verify>
  <done>Schema integration tests cover all 13 schemas across documents, plaid, and claude with valid inputs, invalid inputs, and edge cases.</done>
</task>

<task type="auto">
  <name>Task 3: Update jest.config.js and verify full coverage</name>
  <files>backend-express/jest.config.js</files>
  <action>
    1. Add validation files to collectCoverageFrom in jest.config.js:
       - Add `'schemas/**/*.js'` to the array
       - `middleware/**/*.js` already includes validate.js (added in 02-03)

    2. Run full test suite with coverage:
       `cd backend-express && npx jest --coverage`

    3. Verify:
       - All tests pass (middleware, routes, validation)
       - Coverage on middleware/validate.js: 90%+ statements
       - Coverage on schemas/*.js: 90%+ statements
       - No regressions in auth tests

    4. If coverage is below 90% on any validation file, add targeted tests to close the gap.
  </action>
  <verify>`cd backend-express && npx jest --coverage` — all tests pass, validation files at 90%+ statement coverage</verify>
  <done>Full test suite passes. Validation middleware and schemas have 90%+ statement coverage. No regressions.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `backend-express/__tests__/middleware/validate.test.js` exists with 8+ unit tests
- [ ] `backend-express/__tests__/validation/schemas.test.js` exists with tests for all 13 schemas
- [ ] jest.config.js includes `schemas/**/*.js` in coverage collection
- [ ] `cd backend-express && npx jest --coverage` — all tests pass
- [ ] Validation code at 90%+ statement coverage
- [ ] Auth tests still pass (no regressions)
</verification>

<success_criteria>
- validate.test.js covers all middleware branches (valid, invalid, stripUnknown, coercion, sources)
- schemas.test.js covers all 13 schemas with valid, invalid, and edge case inputs
- 90%+ statement coverage on middleware/validate.js and all schema files
- All existing tests pass (auth, integration)
</success_criteria>

<output>
After completion, create `.planning/phases/03-input-validation-framework/03-05-SUMMARY.md`
</output>
