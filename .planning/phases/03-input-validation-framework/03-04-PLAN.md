---
phase: 03-input-validation-framework
plan: 04
type: execute
domain: validation
---

<objective>
Define Joi schemas for all Claude AI endpoints and wire them into route handlers, replacing inline validation.

Purpose: Apply consistent Joi validation to Claude AI analysis endpoints.
Output: `backend-express/schemas/claude.js` with schemas wired into `backend-express/routes/claude.js`.
</objective>

<context>
@.planning/PROJECT.md
@backend-express/middleware/validate.js (created in 03-01)
@backend-express/routes/claude.js
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create Claude AI endpoint Joi schemas</name>
  <files>backend-express/schemas/claude.js</files>
  <action>
    Create schemas for the Claude endpoints that accept input:

    1. `analyzeSchema` (body) for POST /v1/ai/claude/analyze:
       - `prompt`: string, optional, trimmed
       - `documentText`: string, optional, trimmed
       - At least one of `prompt` or `documentText` must be present. Use Joi.object().or('prompt', 'documentText') to enforce this — Joi's `or()` means "at least one must exist"
       - `model`: string, optional, trimmed, default('claude-3-5-sonnet-20241022')
       - `maxTokens`: number, optional, integer, min(1), max(100000), default(4096)
       - `temperature`: number, optional, min(0), max(1), default(0.1)
       - `documentType`: string, optional, trimmed

    Note: The /test endpoint takes no body input, so no schema needed for it.

    Use CommonJS exports.
  </action>
  <verify>`node -e "const s = require('./backend-express/schemas/claude.js'); console.log(Object.keys(s));"` prints array with analyzeSchema</verify>
  <done>claude.js exports analyzeSchema with or('prompt', 'documentText') constraint.</done>
</task>

<task type="auto">
  <name>Task 2: Wire schema into Claude routes</name>
  <files>backend-express/routes/claude.js</files>
  <action>
    Replace inline validation in claude.js with Joi middleware:

    1. Add imports at top:
       - `const { validate } = require('../middleware/validate');`
       - `const { analyzeSchema } = require('../schemas/claude');`

    2. Wire schema on analyze endpoint:
       - `router.post('/analyze', validate(analyzeSchema), async (req, res, next) => { ... })`

    3. REMOVE the inline validation block:
       - Remove the `if (!prompt && !documentText)` block (lines 12-16)

    4. The handler can now trust that at least one of prompt/documentText exists (Joi enforces this).

    5. Keep the /test endpoint unchanged — it takes no body input.

    6. Keep the rest of the handler unchanged (buildMortgageAnalysisPrompt logic, service calls, error handling).
  </action>
  <verify>`cd backend-express && npm test` — all existing tests pass. Then `node -e "require('./backend-express/routes/claude.js')"` loads without error.</verify>
  <done>Claude analyze route uses Joi validation middleware. Inline validation removed. Test endpoint unchanged. Existing tests pass.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `backend-express/schemas/claude.js` exists with analyzeSchema
- [ ] Analyze route uses `validate()` middleware
- [ ] Inline `if (!prompt && !documentText)` check removed
- [ ] Test endpoint unchanged
- [ ] `cd backend-express && npm test` passes all existing tests
</verification>

<success_criteria>
- analyzeSchema enforces at least one of prompt/documentText with Joi.or()
- Schema wired as Express middleware on analyze route
- Inline validation removed
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-input-validation-framework/03-04-SUMMARY.md`
</output>
