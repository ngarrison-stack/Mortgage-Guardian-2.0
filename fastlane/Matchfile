git_url(ENV["MATCH_GIT_URL"])

# Optional: storage mode
# storage_mode("git")

# The app identifier(s)
app_identifier("com.nickgarrison.mortgageguardian")

environment("production")

# If you want to restrict match to a specific branch
# git_branch("certs/main")

# The readonly option is set in the lane when running
